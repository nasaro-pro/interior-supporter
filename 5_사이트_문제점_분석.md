---
title: "인테리어 프로젝트 관리 플랫폼 — ⑤ 사이트 문제점 분석"
subtitle: "구현체 `web/` 를 설계문서 ①②③④ 기준으로 전수 대조한 결과와 해결 지침"
version: "v2.0"
date: "2026-09-13"
scope: "web/ 전 소스 · docs/ARCHITECTURE.md · docs/SITE_DESIGN.md · 문서① 아이디어 가이드 · 문서④ 프롬프트 · .github/workflows"
status: "v1.0 에서 찾은 S1·S2 전건 + 수정 중 새로 발견한 4건 해결. 잔여는 9장"
---

# ⑤ 사이트 문제점 분석

---

## 해결 현황 (v2.0 — 2026-09-13)

v1.0 이 찾은 52건 중 **S1 치명 9건 · S2 중대 13건 전건**과 **S3 기능 미달 13건**을 고쳤다.
수정 과정에서 **v1.0 이 놓친 S1급 결함 4건**이 새로 드러나 함께 고쳤다(9.1절).

### 최종 검증 (TZ=UTC, CI 와 동일 조건)

| 검사 | v1.0 | v2.0 |
|---|---|---|
| `pnpm typecheck` | ✅ | ✅ |
| `pnpm lint` | ❌ error 2 | ✅ error 0 · warning 0 |
| `pnpm selfcheck` | ✅ | ✅ (회귀 방지 규칙 3종 추가) |
| `pnpm test:unit` | 9 passed | **19 passed** |
| `pnpm test:integration` | 39 passed | **51 passed** |
| `pnpm test:e2e` | ❌ T-01 로그인 실패 | ✅ **6/6 passed** (`@critical` 4건 포함) |
| `pnpm db:check` | ✅ | ✅ |
| `pnpm build` | ✅ | ✅ |

> v1.0 의 "출시 불가" 판정 근거였던 CI 게이트가 전부 초록불이 되었다.
> 남은 항목은 9.2절 참조.

---

---

## 0. 문서 개요

### 0.1 목적

이 문서는 **설계문서가 서로 모순되는지**를 보던 이전 문서(v3.0, 부록 C에 보존)를 대체한다.
이제 대조 대상은 문서끼리가 아니라 **문서 ↔ 실제 구현체(`web/`)** 다.

세 가지를 적는다.

1. 지금 사이트가 **어디까지 만들어졌는가** (완성도 정량 평가)
2. **무엇이 잘못되었는가** (결함 전수 목록 — 위치·근거·재현·영향)
3. **어떻게 고치는가** (수정 방안 · 완료 기준 · 회귀 테스트)

### 0.2 범위와 방법

| 구분 | 내용 |
|---|---|
| 정적 분석 | `web/src` 전 파일(앱 라우트 60개, 모듈 15개, lib 20개, 컴포넌트 30개) 읽고 문서 조항과 1:1 대조 |
| 기계 검증 | `typecheck` · `lint` · `selfcheck` · `test:unit` · `test:integration` · `test:e2e` · `db:check` · `build` 실제 실행 |
| 동적 검증 | 의심 결함에 대해 **탐침(probe) 통합 테스트를 작성해 실제 DB에서 재현** 후 삭제 |
| 제외 | UI 시각 디자인 품질, 카피라이팅, 가격 타당성, 법률 자문 영역의 적법성 판단 |

> 이 문서가 "재현 확인"이라고 적은 항목은 **실제로 코드를 돌려 결과를 받아낸 것**이고, "정적 확인"은 코드 독해로 판정한 것이다. 둘을 구분해 표기했다.

### 0.3 심각도 정의

| 등급 | 정의 | 출시 정책 |
|---|---|---|
| **S1 · 치명** | 데이터 격리·권한·정합성이 깨지거나, 핵심 흐름이 동작하지 않거나, CI 게이트가 통과하지 못함 | **전건 해결 전 출시 금지** |
| **S2 · 중대** | 운영 중 사고·오기록·장애를 유발. 우회 수단이 없거나 비용이 큼 | 출시 전 해결 권장, 최소한 완화책 + 이슈 등록 |
| **S3 · 기능 미달** | 설계문서가 약속한 기능이 없거나 사용 불가 수준 | 파일럿 오픈 전 해결 |
| **S4 · 규약 위반·부채** | 동작은 하지만 `AGENTS.md`·아키텍처 규칙 위반. 방치 시 S1을 재생산 | 다음 리팩터 사이클 |

### 0.4 결함 ID 규칙

`S{등급}-{일련번호}` . 이 ID는 커밋 메시지·이슈·테스트 이름에 그대로 인용한다.
예: `fix(access): S1-03 verifyCodeAction await 누락`

### 0.5 요약 대시보드

| 등급 | 건수 | 대표 항목 |
|---|---|---|
| S1 치명 | **9** | 프로젝트 경계 미검증, 2번째 업체 개설 불가, KST 9시간 오차, CI 전면 불통 |
| S2 중대 | **13** | 가짜 staff 컨텍스트, 초대 이메일 미바인딩, 블록 예약공개 500, 업로드 CORS |
| S3 기능 미달 | **16** | 현재 공정 입력 불가, 홈 CMS 편집기 골격, plan gate 전무, 랜딩 스텁 |
| S4 규약·부채 | **14** | 빈 파일 32개, 메시지 하드코딩, 스코프 누락 쿼리 |
| **합계** | **52** | |

**한 줄 총평 —**
토대(스키마·테넌시·인가·감사)는 문서를 성실히 따라 잘 깔렸다. 그러나 **"업체 경계" 아래 한 단계인 "프로젝트 경계"가 통째로 비어 있고**, 간판 기능인 자유 배치형 CMS 편집기와 운영 계층(관측성·보안 헤더·요금제 스위치)은 데모 수준이다.

---

## 1. 기계 검증 실행 결과

2026-09-13 기준, 로컬(Windows · Postgres 17 · Node 20 · pnpm 11.24.0)에서 실제 실행한 결과다.

| 명령 | 결과 | 비고 |
|---|---|---|
| `pnpm typecheck` | ✅ PASS | 오류 0 |
| `pnpm selfcheck` | ✅ PASS | 벤더 격리·`any`·middleware 파일명 규칙 통과 |
| `pnpm test:unit` | ✅ 9 passed / 3 files | 커버리지 얕음 (§5 S3-16) |
| `pnpm test:integration` | ✅ 39 passed / 5 files | T-02·05·06·07·09·10·11·13·14·15·16 존재 |
| `pnpm db:check` | ✅ Everything's fine | 스키마 드리프트 없음 |
| `pnpm build` | ✅ PASS | 전 라우트 컴파일 성공 |
| **`pnpm lint`** | ❌ **FAIL** | error 2 · warning 3 → **S1-07** |
| **`pnpm test:e2e`** | ❌ **미통과** | `test-results/` 직전 실행 기록에서 T-01이 로그인 단계 실패 → **S1-08** |

### 1.1 lint 실패 상세

```
src/app/invite/[token]/page.tsx
  16:68  error  Cannot call impure function during render (`Date.now`)  react-hooks/purity

src/modules/page-builder/service.ts
  485:9  error  'yOffset' is never reassigned. Use 'const' instead       prefer-const

warning:
  src/app/app/[companySlug]/projects/[projectId]/materials/page.tsx:91  no-img-element
  src/app/app/[companySlug]/projects/[projectId]/photos/page.tsx:65     no-img-element
  src/modules/audit/repo.ts:1:30  'isNull' is defined but never used
```

`ARCHITECTURE.md` 19.5 CI 게이트가 `pnpm lint`를 필수 단계로 두고 있으므로, **현재 상태로는 어떤 브랜치도 병합 조건을 충족하지 못한다.**

### 1.2 E2E 실패 상세

`web/test-results/critical-T-01-.../error-context.md` 에 남은 기록:

```
TimeoutError: page.waitForURL: Timeout 20000ms exceeded.
  navigated to "http://localhost:3001/login"
페이지 스냅샷: "이메일 또는 비밀번호가 올바르지 않습니다."
```

시드가 만든 계정으로 로그인 자체가 실패했다. `@critical` 태그가 붙은 T-01~T-04가 CI 필수 통과 조건이므로 **CI는 여기서도 멈춘다.**

---

## 2. 구현 완성도 평가

각 영역을 "설계문서가 요구한 것 대비 몇 %인가"로 매겼다.

| 영역 | 근거 조항 | 완성도 | 판정 근거 |
|---|---|---|---|
| DB 스키마 | ③ 5.1~5.9 | **95%** | 15개 모듈 테이블 전부 존재, 컬럼·인덱스 거의 1:1. `drizzle/manual/0001_checks.sql`이 I2·I8·I9를 DB CHECK로 강제 |
| 테넌시 골격 | ③ 6.1~6.5 | **85%** | `DataContext`·`scoped`·`assertSameTenant`·`requireXxx` 7종 전부 존재. **단 프로젝트 축 누락(S1-01)** |
| 인증·계정 | ③ 7.1~7.8 | **85%** | Better Auth 복수형 어댑터·UUID PK·쿠키캐시 OFF·이메일 인증 필수·`input:false` 전부 충실 |
| 초대·확정 코드 | ③ 7.5~7.6 | **85%** | 8자리 혼동문자 제외 코드·SHA-256 해시·`codeVersion` 세대 무효화·10분 5회 제한 구현 |
| 감사 로그 | ③ 12장 | **70%** | 같은 트랜잭션 기록·binding 본문 보존·마스킹 존재. IP/UA 미기록, 라벨 불일치 |
| 파일·미디어 | ③ 10장 | **75%** | presign·매직바이트·격리·썸네일·SSRF 방어 구현. 프로덕션 CORS 미검증 |
| 콘텐츠 4종 | ③ 8.3~8.6 | **70%** | 서비스 로직 충실, 화면은 최소 폼 |
| 알림 | ③ 11장 | **65%** | 어댑터·dedupe·재시도 구현. 수신자 판정에 가짜 컨텍스트 사용 |
| 고객 포털 | ② 3-E, 4.7 | **75%** | 탭·브랜딩·모바일 하단 네비까지 구현. 댓글 수정 불가 |
| **홈 CMS 편집기** | ② 4.2 / ③ 9장 | **30%** | §5 S3-02 참조 — 간판 기능이 가장 미완 |
| 플랫폼 콘솔 | ② 3-B / ③ 4.1 | **40%** | 6개 화면 존재하나 목록 렌더 수준 |
| 마케팅·법무 | ② 3-A | **15%** | 랜딩 5줄 스텁, 요금제 1문장 |
| 관측성·운영 | ③ 17장 | **10%** | 구조화 로깅·requestId·알람 전무 |
| 보안 헤더 | ③ 14.2 | **0%** | CSP·XCTO·Referrer-Policy·HSTS 전무 |
| plan gate | ③ 13.1 | **0%** | `billing/gate.ts` 자체가 없음 |
| 개인정보 대응 | ③ 15.4 | **90%** | `scripts/privacy/{export,destroy}.mjs` + `RUNBOOK_PRIVACY.md` 존재 ✅ |

---

## 3. S1 — 치명 결함 (출시 차단)

---

### S1-01 · 프로젝트 경계 미검증 — 같은 업체 안에서 남의 프로젝트를 조작할 수 있다

| 항목 | 내용 |
|---|---|
| **심각도** | S1 (최우선) |
| **검증** | 🔬 **재현 확인** (탐침 통합 테스트 2건 실행) |
| **위치** | `web/src/modules/*/repo.ts` 전체 `findXById()` · `web/src/modules/*/service.ts` 전체 |
| **문서 근거** | ③ 6.5 (고객 컨텍스트는 `projectId`를 가진다) · ③ 부록 C (`/portal/{id}/*` = `requireProjectAccess(id)`) · ③ 5.9 I6 · ② 2.2 권한 매트릭스 · ③ 20.2 T-12 |

#### 증상

`DataContext`는 `company_id` 한 축만 강제한다. `customer` 컨텍스트에는 `projectId`가 들어 있고 `staff` 컨텍스트는 `loadProjectStaff()`로 담당 프로젝트를 확인하지만, **정작 리소스를 꺼낼 때 그 값을 다시 대조하지 않는다.**

```ts
// web/src/modules/design/repo.ts:47
export async function findDesignById(ctx, id, tx = db) {
  const [row] = await tx.select().from(designVersions)
    .where(and(
      eq(designVersions.id, id),
      scoped(ctx, designVersions),        // ← company_id 만 본다
      isNull(designVersions.deletedAt),
    )).limit(1);
  if (row) assertSameTenant(ctx, row);    // ← company_id 만 본다
  return row ?? null;
}
```

`setDesignApproval`(`design/service.ts:107`)은 `ctx.verified`만 확인하고 `row.projectId === ctx.projectId`를 보지 않는다.

#### 재현 결과 (실제 실행)

```
CROSS_PROJECT_APPROVAL_ALLOWED = true
PM_CROSS_PROJECT_WRITE_ALLOWED = true
```

**시나리오 A — 고객의 교차 프로젝트 승인**
1. 고객 X가 업체 A의 프로젝트 A1에만 `project_access` + `project_verifications` 보유
2. 업체 A의 다른 프로젝트 A2(담당 PM 다름)에 공개된 디자인 D가 있음
3. X가 `approveDesignAction(projectId = A1, id = D)` 호출
4. `requireProjectVerified("A1")` 통과 → `setDesignApproval(ctx, D)` → **A2 디자인이 승인됨**
5. 감사 로그에는 `projectId = A2`, `actorId = X` 로 기록 → **분쟁 근거 자체가 오염**

**시나리오 B — PM의 교차 프로젝트 공개**
1. PM P는 프로젝트 A2 담당
2. `loadProjectStaff(P, A2)` 로 정상 컨텍스트 획득
3. 그 컨텍스트로 `changeDesignVisibility(ctx, <A1의 디자인>, "published")` 호출
4. **담당도 아닌 A1의 디자인이 고객에게 공개됨**

#### 영향 범위

같은 패턴이 아래 전부에 존재한다.

| 모듈 | 함수 | 노출 행위 |
|---|---|---|
| design | `findDesignById` | 타 프로젝트 디자인 승인·반려·공개상태 변경 |
| material | `findMaterialById` | 타 프로젝트 자재 승인·구매상태 변경·공개 |
| comment | `findCommentById` | 타 프로젝트 댓글 수정·삭제 |
| photo | `findPhotoById` | 타 프로젝트 사진 공개·삭제·페어링 |
| schedule | `findScheduleById` | 타 프로젝트 일정 수정·공개 |
| page-builder | `findPageByProject`, `updateBlockRow` | 타 프로젝트 홈 블록 수정 |
| storage-quota | `findStorageObjectById` | 타 프로젝트 업로드 객체 완료 처리 |

E2E `T-12`는 **URL 접근만** 검사하므로 이 경로를 잡지 못한다. 통합 `T-02`는 **업체 간** 교차만 본다.

#### 원인

문서 6.2 "방어선" 표가 `company_id` 한 축만 명시했고, `DataContext`의 `projectId` 필드가 **인가 판정에만 쓰이고 데이터 접근 조건으로는 전달되지 않았다.**

#### 해결 방안

**1) 공통 헬퍼 추가 — `web/src/lib/tenancy/context.ts`**

```ts
/** company 축에 더해 project 축까지 강제한다. customer 는 자기 프로젝트만, staff 는 담당 프로젝트만. */
export function assertSameProject(
  ctx: DataContext,
  row: { projectId: string | null },
): void {
  if (ctx.kind === "system" || ctx.kind === "platform") return;
  if (ctx.kind === "customer") {
    if (!row.projectId || row.projectId !== ctx.projectId) {
      throw new TenantViolationError();
    }
  }
  // staff 는 service 계층에서 loadProjectStaff 재확인 (아래 2번)
}

/** repo 쿼리에 project 조건을 직접 얹는다. */
export function projectScoped<T extends { projectId: PgColumn }>(
  ctx: DataContext,
  table: T,
) {
  if (ctx.kind !== "customer") return undefined;
  return eq(table.projectId, ctx.projectId);
}
```

**2) 모든 `findXById` 에 적용**

```ts
export async function findDesignById(ctx: DataContext, id: string, tx = db) {
  const [row] = await tx.select().from(designVersions)
    .where(and(
      eq(designVersions.id, id),
      scoped(ctx, designVersions),
      projectScoped(ctx, designVersions),   // ← 추가
      isNull(designVersions.deletedAt),
    )).limit(1);
  if (row) { assertSameTenant(ctx, row); assertSameProject(ctx, row); }  // ← 추가
  return row ?? null;
}
```

**3) staff 경로는 service 에서 담당 재확인**

`changeDesignVisibility` 등 `id`만 받는 staff 함수는 행을 읽은 직후 담당 여부를 다시 판정한다.

```ts
// web/src/lib/authz/index.ts 에 추가
export async function assertStaffOwnsProject(ctx: DataContext, projectId: string) {
  if (ctx.kind !== "staff") return;
  if (ctx.roles.includes("company_admin")) return;   // 총관리자는 전체
  const row = await findProjectStaff(ctx.userId, projectId);
  if (!row || row.managerId !== ctx.userId) throw new ForbiddenError();
}
```

```ts
export async function changeDesignVisibility(ctx, id, to, publishAt?) {
  assertEditor(ctx);
  const row = await findDesignById(ctx, id);
  if (!row) throw new NotFoundError();
  await assertStaffOwnsProject(ctx, row.projectId);   // ← 추가
  ...
}
```

**4) 액션 계층 인자 정리 (권장)**
장기적으로는 액션이 `(projectId, resourceId)` 두 값을 받고 서비스가 `resource.projectId !== projectId` 이면 즉시 `NotFoundError`를 던지는 형태가 더 견고하다.

#### 완료 기준

- [ ] `projectScoped` / `assertSameProject` / `assertStaffOwnsProject` 추가
- [ ] design·material·comment·photo·schedule·page-builder·storage-quota 의 `findXById`·`updateXRow` 전부 적용
- [ ] 아래 회귀 테스트 2건 추가(§8 R-01, R-02)하고 **실패 → 수정 → 통과** 확인
- [ ] `pnpm test:integration` 통과

---

### S1-02 · 한 계정이 업체를 두 개 만들 수 없다

| 항목 | 내용 |
|---|---|
| **심각도** | S1 |
| **검증** | 🔬 **재현 확인** |
| **위치** | `web/src/modules/notification/repo.ts:25` · 호출부 `web/src/modules/company/service.ts:79` |
| **문서 근거** | ③ ADR-10 · ③ 5.4 (`memberships` 는 한 계정이 여러 업체) · ② 0.2 멤버십 정의 · ① 4.2 "한 사람이 여러 업체에서 서로 다른 역할" |

#### 증상

```
SECOND_COMPANY = ERROR: Failed query: insert into "notification_preferences"
  ("id","user_id","event_type","inapp_enabled","email_enabled","created_at") values ...
```

#### 원인

```ts
// web/src/modules/notification/repo.ts:25
export async function insertDefaultPreferences(ctx, userId, tx = db) {
  void ctx;
  await tx.insert(notificationPreferences).values(
    DEFAULT_EVENTS.map((eventType) => ({ userId, eventType, ... })),
  );            // ← onConflict 처리 없음
}
```

`notification_preferences` 는 `(user_id, event_type)` 유니크(`notif_pref_uq`). 두 번째 `createCompany()` 호출 시 16건 전부 충돌 → `db.transaction` 전체 롤백 → **업체 개설 실패.**

#### 영향

계정 모델의 핵심 전제가 무너진다. 프랜차이즈·다지점 업체, 대행 PM, 파일럿 운영자 본인 계정 모두 막힌다. 오류 메시지는 `messages.internalError`("처리할 수 없습니다")로만 보여 원인 추적도 어렵다.

#### 해결 방안

```ts
export async function insertDefaultPreferences(ctx: DataContext, userId: string, tx = db) {
  void ctx;
  await tx
    .insert(notificationPreferences)
    .values(DEFAULT_EVENTS.map((eventType) => ({
      userId,
      eventType,
      inappEnabled: true,
      emailEnabled: eventType !== "photo.published",
    })))
    .onConflictDoNothing({
      target: [notificationPreferences.userId, notificationPreferences.eventType],
    });
}
```

부수 확인 사항: `createCompany()` 의 slug 생성도 `slugTaken()` 확인 후 1회만 접미사를 붙인다(`company/service.ts:40`). 동시 개설 레이스 대비로 유니크 위반 시 1회 재시도 루프를 넣는 것이 안전하다.

#### 완료 기준
- [ ] `onConflictDoNothing` 적용
- [ ] 회귀 테스트 R-03 추가 (§8)

---

### S1-03 · 확정 코드 잠금 처리가 죽은 코드 (`await` 누락)

| 항목 | 내용 |
|---|---|
| **심각도** | S1 |
| **검증** | 정적 확인 (통합 테스트가 서비스 함수를 직접 호출해 우회하고 있어 미검출) |
| **위치** | `web/src/modules/access/actions.ts:56` |
| **문서 근거** | ③ 7.6 V4 "10분 5회 초과 시 잠금 + 알림" · ② 9장 남용 방지 |

#### 증상

```ts
export async function verifyCodeAction(projectId: string, formData: FormData) {
  const ctx = await requireProjectAccess(projectId);
  try {
    return verifyProjectCode(          // ← await 없음. Promise 를 그대로 반환
      ctx, projectId, String(formData.get("code") ?? ""),
      await requestIp(await headers()),
    );
  } catch (error) {
    if (error instanceof RateLimitError) {
      return { ok: false as const, remaining: 0 };   // ← 절대 실행되지 않음
    }
    throw error;
  }
}
```

`try` 블록 안에서 Promise를 `await` 없이 반환하면 이후의 거부(rejection)는 `catch`에 잡히지 않는다.

#### 영향

6회째 오입력에서 `verifyProjectCode` 가 `RateLimitError`를 던지면 → 서버 액션이 그대로 실패 → 클라이언트 `VerifyForm`의 `setState(await action(formData))`가 거부됨 → **`messages.verifyLocked`("잠겼습니다") 안내 대신 알 수 없는 오류 화면.** 문서가 요구한 잠금 UX가 사용자에게 전달되지 않는다.

#### 해결 방안

```ts
export async function verifyCodeAction(projectId: string, formData: FormData) {
  const ctx = await requireProjectAccess(projectId);
  try {
    return await verifyProjectCode(        // ← await 추가
      ctx, projectId, String(formData.get("code") ?? ""),
      await requestIp(await headers()),
    );
  } catch (error) {
    if (error instanceof RateLimitError) return { ok: false as const, remaining: 0 };
    throw error;
  }
}
```

**같은 패턴 전수 점검 필요** — `return <asyncFn>(...)` 이 `try` 안에 있는 모든 곳.

```bash
grep -rn "    return [a-zA-Z]*(" web/src/modules/*/actions.ts
```

#### 완료 기준
- [ ] `await` 추가
- [ ] 회귀 테스트 R-04: 6회 오입력 후 액션이 `{ok:false, remaining:0}`을 **반환**하는지(던지지 않는지) 검증

---

### S1-04 · 날짜·시각 입력이 서버 로컬 타임존으로 해석됨 (KST 9시간 오차)

| 항목 | 내용 |
|---|---|
| **심각도** | S1 |
| **검증** | 정적 확인 (동작 원리상 결정적) |
| **위치** | `schedule/service.ts:63,64,85,86` · `design/actions.ts:41` · `material/actions.ts:56` · `schedule/actions.ts:64` · `admin/audit-log/page.tsx:56-57` |
| **문서 근거** | ③ 5.1 "시각은 timestamptz, 저장 UTC, 표시 Asia/Seoul" · `AGENTS.md` §8 "날짜 표시는 Asia/Seoul 기준으로 통일" |

#### 증상

```ts
// web/src/modules/schedule/service.ts:63
startAt: new Date(input.startAt),      // input.startAt = "2026-09-13T10:00"
```

HTML `<input type="datetime-local">` 은 **타임존 없는 문자열**을 보낸다. `new Date("2026-09-13T10:00")` 은 이를 **실행 환경의 로컬 타임존**으로 해석한다.

| 환경 | 해석 | DB 저장 | 화면 표시(KST) |
|---|---|---|---|
| 개발자 로컬(KST) | 10:00 KST | `01:00Z` | 10:00 ✅ |
| **Vercel 프로덕션(UTC)** | 10:00 UTC | `10:00Z` | **19:00 ❌** |

`<input type="date">`도 같다. `new Date("2026-09-13")`은 **UTC 자정**으로 고정 해석되어, KST 기준 필터가 9시간 어긋난다.

#### 영향 범위

| 기능 | 파일 | 결과 |
|---|---|---|
| 일정 등록·수정 | `schedule/service.ts:63,64,85,86` | 모든 공사·방문·확정 일정이 9시간 밀림 |
| **예약 공개 `publishAt`** | `design/actions.ts:41`, `material/actions.ts:56`, `schedule/actions.ts:64` | 예약한 시각보다 9시간 늦게 공개 |
| 감사 로그 기간 필터 | `admin/audit-log/page.tsx:56-57` | 하루 경계가 어긋나 조회 누락 |
| 프로젝트 계약일·착공일·준공일 | `projects/new`, `admin/projects` (`date` 컬럼) | `date` 타입이라 영향 작으나 일관성 필요 |
| 사진 촬영일 `shotDate` | `photos/page.tsx:47` | 동일 |

로컬에서는 KST라 정상으로 보이므로 **개발 중에는 절대 드러나지 않고 배포 직후 전면적으로 터진다.**

#### 해결 방안

**1) 변환 유틸 추가 — `web/src/lib/datetime.ts`**

```ts
import { formatInTimeZone, fromZonedTime, toZonedTime } from "date-fns-tz";

export const SEOUL = "Asia/Seoul";

/** datetime-local / date 입력 문자열을 'KST 기준 시각'으로 읽어 UTC Date 로 변환한다. */
export function parseSeoulInput(value: string): Date {
  if (!value) throw new Error("빈 날짜 입력");
  // "2026-09-13T10:00" | "2026-09-13"
  const normalized = value.length === 10 ? `${value}T00:00:00` : value;
  return fromZonedTime(normalized, SEOUL);
}

/** 하루의 끝(KST 23:59:59.999)을 UTC 로 — 기간 필터의 to 경계용 */
export function parseSeoulDayEnd(value: string): Date {
  return fromZonedTime(`${value}T23:59:59.999`, SEOUL);
}

/** Date -> datetime-local input value (KST) */
export function toSeoulInputValue(date: Date): string {
  return formatInTimeZone(date, SEOUL, "yyyy-MM-dd'T'HH:mm");
}
```

**2) 적용**

```ts
// schedule/service.ts
startAt: parseSeoulInput(input.startAt),
endAt: input.endAt ? parseSeoulInput(input.endAt) : null,

// design/actions.ts / material/actions.ts / schedule/actions.ts
publishAtRaw ? parseSeoulInput(publishAtRaw) : undefined,

// admin/audit-log/page.tsx
from: q.from ? parseSeoulInput(q.from) : undefined,
to:   q.to   ? parseSeoulDayEnd(q.to)  : undefined,
```

**3) 기존 화면의 기본값 렌더링도 `toSeoulInputValue()` 사용**

**4) 회귀 방지** — `selfcheck.mjs` 에 규칙 추가:
```js
// Server Action / service 에서 new Date(<문자열 변수>) 직접 호출 금지
if (/new Date\((?!\)|Date\.now)[a-z]/i.test(line) && /(actions|service)\.ts$/.test(rel)) {
  fail(`${rel}:${i+1}: 입력 문자열은 parseSeoulInput() 으로 변환할 것 (S1-04)`);
}
```

#### 완료 기준
- [ ] 유틸 추가 및 6개 지점 전부 교체
- [ ] `TZ=UTC pnpm test:integration` 으로 회귀 테스트 R-05 통과
- [ ] `selfcheck` 규칙 추가

---

### S1-05 · 커서 페이지네이션이 행을 조용히 삼킨다

| 항목 | 내용 |
|---|---|
| **심각도** | S1 (감사 로그에서 **결정적으로 발생**) |
| **검증** | 정적 확인 + 원리상 필연 |
| **위치** | `audit/repo.ts:64` · `project/repo.ts:116` · `customer/repo.ts:20` · `company/repo.ts:117` |
| **문서 근거** | ③ 16.2-1 "커서 기반 페이지네이션(**created_at + id**)" · `AGENTS.md` §8 |

#### 증상

```ts
.where(and(
  ...,
  filter.cursor ? lt(auditLogs.createdAt, new Date(filter.cursor)) : undefined,
))
.orderBy(desc(auditLogs.createdAt), desc(auditLogs.id))
.limit(limit + 1);
...
nextCursor: hasMore ? items[items.length - 1]?.log.createdAt.toISOString() : null
```

정렬은 `(createdAt, id)` 복합인데 **커서는 `createdAt` 단일**이고 비교가 `lt`(strict)다.
마지막 항목과 `created_at`이 같은 행은 **다음 페이지에서 통째로 건너뛴다.**

#### 왜 감사 로그에서 반드시 터지는가

PostgreSQL의 `now()`(= `defaultNow()`)는 **트랜잭션 시작 시각**을 반환한다. `recordAudit`은 문서 12.1에 따라 **비즈니스 트랜잭션과 같은 트랜잭션**에서 기록되므로, 한 트랜잭션에서 남긴 여러 감사 레코드는 **`created_at`이 바이트 단위로 동일**하다.

예: `saveHomeBlocks()` 가 블록 5개의 `visibility_change`를 한 트랜잭션에 남기면 5건이 같은 타임스탬프. 페이지 경계가 그 사이에 걸리면 **나머지 건이 영구히 조회되지 않는다.** 분쟁 근거로 쓰는 기록에서 치명적이다.

`ISO 8601` 문자열 왕복 과정에서 마이크로초가 잘리는 문제도 겹친다(Postgres는 μs, `toISOString()`은 ms).

#### 해결 방안

**1) 복합 커서 인코딩 유틸 — `web/src/lib/cursor.ts` (신규)**

```ts
export type Cursor = { createdAt: Date; id: string };

export function encodeCursor(c: Cursor): string {
  return Buffer.from(`${c.createdAt.toISOString()}|${c.id}`, "utf8").toString("base64url");
}

export function decodeCursor(raw: string): Cursor | null {
  try {
    const [ts, id] = Buffer.from(raw, "base64url").toString("utf8").split("|");
    if (!ts || !id) return null;
    const createdAt = new Date(ts);
    return Number.isNaN(createdAt.getTime()) ? null : { createdAt, id };
  } catch { return null; }
}
```

**2) 조건을 튜플 비교로**

```ts
import { or, and, eq, lt, desc, sql } from "drizzle-orm";

const c = filter.cursor ? decodeCursor(filter.cursor) : null;

.where(and(
  scoped(ctx, auditLogs),
  c ? or(
        lt(auditLogs.createdAt, c.createdAt),
        and(eq(auditLogs.createdAt, c.createdAt), lt(auditLogs.id, c.id)),
      )
    : undefined,
  ...
))
.orderBy(desc(auditLogs.createdAt), desc(auditLogs.id))
.limit(limit + 1);

// 반환
nextCursor: hasMore && last ? encodeCursor({ createdAt: last.log.createdAt, id: last.log.id }) : null
```

> Postgres 행 비교 `(created_at, id) < ($1, $2)` 를 `sql` 로 써도 되고 인덱스도 잘 타지만, Drizzle 이식성을 위해 `or/and` 형태를 권한다.

**3) 커버링 인덱스 확인**
`audit_company_created_idx (company_id, created_at)` 에 `id`가 포함되지 않는다. 정렬 안정성은 PK로 보장되므로 필수는 아니나, 필요 시 `(company_id, created_at DESC, id DESC)` 로 조정한다.

#### 완료 기준
- [ ] `lib/cursor.ts` 추가, 4개 repo 전부 교체
- [ ] 회귀 테스트 R-06: 같은 트랜잭션에서 감사 5건 기록 후 `limit=2`로 3페이지 순회 → 총 5건 전부 나오는지

---

### S1-06 · 보안 응답 헤더가 하나도 설정되지 않았다

| 항목 | 내용 |
|---|---|
| **심각도** | S1 |
| **검증** | 정적 확인 (`next.config.ts` 에 `headers()` 없음, `proxy.ts` 도 헤더를 붙이지 않음) |
| **위치** | `web/next.config.ts` |
| **문서 근거** | ③ 14.2 "헤더 — CSP, X-Content-Type-Options, Referrer-Policy" · ③ 14.2 "전송 — 전 구간 HTTPS, HSTS" |

#### 증상

현재 `next.config.ts` 는 `images` 설정과 Sentry 래핑만 한다. 응답에 **CSP · X-Content-Type-Options · Referrer-Policy · Strict-Transport-Security · X-Frame-Options / frame-ancestors 가 전부 없다.**

#### 왜 이 사이트에서 특히 위험한가

- 텍스트 블록이 `dangerouslySetInnerHTML` 로 렌더된다(`home-renderer.tsx:71`). 저장 시 `sanitize-html`을 통과시키지만(`page-builder/service.ts:69`) 이는 **단일 방어선**이다. CSP가 없으면 sanitize 우회 한 건이 곧바로 고객 계정 탈취로 이어진다.
- 고객 포털이 업체 브랜드 색상을 인라인 `style`로 주입한다(`portal/[projectId]/layout.tsx:36`).
- `frame-ancestors` 부재 → 클릭재킹으로 승인/확정지시 버튼을 눌리게 할 수 있다. **확정 지시는 수정·삭제가 불가능한 기록**이라 피해가 되돌려지지 않는다.

#### 해결 방안

```ts
// web/next.config.ts
const securityHeaders = [
  { key: "X-Content-Type-Options", value: "nosniff" },
  { key: "Referrer-Policy", value: "strict-origin-when-cross-origin" },
  { key: "X-Frame-Options", value: "DENY" },
  { key: "Permissions-Policy", value: "camera=(), microphone=(), geolocation=()" },
  {
    key: "Strict-Transport-Security",
    value: "max-age=63072000; includeSubDomains; preload",
  },
  {
    key: "Content-Security-Policy",
    value: [
      "default-src 'self'",
      // Next 인라인 부트스트랩 때문에 'unsafe-inline' 필요. nonce 도입 시 제거한다.
      "script-src 'self' 'unsafe-inline'",
      "style-src 'self' 'unsafe-inline'",
      "img-src 'self' data: blob:",          // 이미지는 전부 /api/files 내부 경로
      "font-src 'self' data:",
      "connect-src 'self'",
      "frame-ancestors 'none'",
      "form-action 'self'",
      "base-uri 'self'",
      "object-src 'none'",
    ].join("; "),
  },
];

const nextConfig: NextConfig = {
  images: { /* 기존 */ },
  async headers() {
    return [{ source: "/:path*", headers: securityHeaders }];
  },
};
```

**주의**
- Sentry를 켜면 `connect-src`에 Sentry 인제스트 도메인을 추가해야 한다.
- 화이트라벨 커스텀 도메인(S3-04) 도입 시 `frame-ancestors`·`form-action` 재검토.
- 우선 `Content-Security-Policy-Report-Only` 로 1주 관찰 후 강제 전환을 권한다.

#### 완료 기준
- [ ] 헤더 추가 후 `curl -I` 로 전 응답에 적용 확인
- [ ] 포털·편집기·업로드 전 경로 스모크 (CSP 위반 콘솔 0건)

---

### S1-07 · `pnpm lint` 실패로 CI 게이트가 통과하지 못한다

| 항목 | 내용 |
|---|---|
| **심각도** | S1 |
| **검증** | 🔬 실행 확인 (exit 1) |
| **위치** | `web/src/app/invite/[token]/page.tsx:16` · `web/src/modules/page-builder/service.ts:485` |
| **문서 근거** | ③ 19.5 CI 게이트 · `.github/workflows/ci.yml:26` |

#### 해결 방안

**(1) `invite/[token]/page.tsx:16` — 렌더 중 `Date.now()`**

```ts
// 현재
if (!invite || invite.acceptedAt || invite.expiresAt.getTime() < Date.now()) {

// 수정: 만료 판정을 데이터 계층으로 내린다
// web/src/lib/authz/index.ts
export async function lookupInvitation(token: string) {
  await enforceRateLimit("invite_ip", await requestIp(await headers()));
  const invite = await findInvitationByToken(systemJob("invite-token"), token);
  if (!invite) return null;
  const expired = invite.acceptedAt !== null || invite.expiresAt.getTime() < Date.now();
  return { ...invite, expired };
}

// page.tsx
if (!invite || invite.expired) { ... }
```
`lookupInvitation`은 서버에서 `await` 되는 비동기 함수이므로 purity 규칙 대상이 아니다.

**(2) `page-builder/service.ts:485` — `let yOffset` → `const yOffset`**

```ts
const yOffset = mode === "append" ? maxY : 0;
```

**(3) 경고 3건도 함께 정리**
- `materials/page.tsx:91`, `photos/page.tsx:65` → `<img>` → `next/image` 로 교체(내부 `/api/files` 경로이므로 `localPatterns` 이미 허용됨)
- `audit/repo.ts:1` → 미사용 `isNull` import 제거

#### 완료 기준
- [ ] `pnpm lint` exit 0 (warning 0 목표)

---

### S1-08 · E2E `@critical` 4종이 통과하지 못한다

| 항목 | 내용 |
|---|---|
| **심각도** | S1 |
| **검증** | 🔬 아티팩트 확인 (`web/test-results/critical-T-01-.../error-context.md`) |
| **위치** | `web/tests/e2e/critical.spec.ts:5-11` · `web/tests/fixtures/seed.ts:46-67` · `web/playwright.config.ts` |
| **문서 근거** | ③ 20.2 T-01~T-04 (`@critical`, CI 필수) |

#### 증상

시드 계정으로 로그인하면 `이메일 또는 비밀번호가 올바르지 않습니다` 가 뜨고 `waitForURL` 이 타임아웃된다. 스냅샷의 호스트가 `localhost:3001` 인 점도 주목할 만하다 — `playwright.config.ts` 의 `baseURL` 은 3000인데 `webServer` 가 포트 충돌로 3001에 뜬 정황이다.

#### 원인 후보 (진단 순서)

1. **포트 불일치** — `reuseExistingServer: true` 인데 3000에 다른 프로세스가 있으면 Next가 3001로 뜨고, 테스트는 3000(또는 3001)의 **다른 DB/env** 를 바라볼 수 있다.
   → `webServer.command`를 `pnpm dev --port 3100` 으로 고정하고 `baseURL`도 `http://localhost:3100` 으로 고정.
2. **레이트리밋 누적** — `login_ip` 10분 30회. `requestIp()` 가 로컬에서 항상 `0.0.0.0` 을 반환하므로(`authz/index.ts:18`) 반복 실행 시 전 계정이 한 버킷을 공유해 금방 소진된다. 다만 이 경우 메시지는 `messages.rateLimited` 여야 하므로 1차 원인은 아니다.
   → 시드가 `rate_limit_counters` 를 비우도록 `ensureSettings()` 에 `DELETE FROM rate_limit_counters` 추가.
3. **비밀번호 해시 규격** — `hashCredentialPassword` 가 `better-auth/crypto`의 `hashPassword` 를 쓰고 `accounts.providerId = "credential"`, `accountId = user.id` 로 넣는다. 규격은 맞아 보이나, better-auth 버전의 해시 파라미터가 바뀌면 검증이 깨진다.
   → 진단용 단위 테스트: `verifyPassword(hash, E2E_PASSWORD)` 가 true인지 직접 확인.
4. **`requireEmailVerification` + 시드** — 시드는 `emailVerified: true` 로 넣으므로 정상.

#### 해결 방안

```ts
// web/playwright.config.ts
const PORT = Number(process.env.PLAYWRIGHT_PORT ?? 3100);
const baseURL = process.env.PLAYWRIGHT_BASE_URL ?? `http://localhost:${PORT}`;

export default defineConfig({
  testDir: "tests/e2e",
  fullyParallel: false,
  use: { ...devices["Desktop Chrome"], baseURL },
  webServer: {
    command: `pnpm dev --port ${PORT}`,
    url: `${baseURL}/api/health`,     // 라우트가 실제로 뜬 뒤에 시작
    reuseExistingServer: !process.env.CI,
    timeout: 180_000,
  },
});
```

```ts
// web/tests/fixtures/seed.ts — ensureSettings() 끝에
await db.execute(sql`DELETE FROM rate_limit_counters`);
```

또한 로그인 헬퍼에 실패 원인을 드러내는 어서션을 넣어 다음 회귀 때 진단이 빨라지게 한다.

```ts
async function login(page: Page, email: string, password: string) {
  await page.goto("/login");
  await page.locator('input[name="email"]').fill(email);
  await page.locator('input[name="password"]').fill(password);
  await page.locator('button[type="submit"]').click();
  await expect(page.getByText(messages.loginFailed)).toHaveCount(0);   // ← 추가
  await page.waitForURL((url) => !url.pathname.startsWith("/login"), { timeout: 20_000 });
}
```

#### 완료 기준
- [ ] `pnpm test:e2e -- --grep @critical` 로컬 통과
- [ ] CI(S1-09 해결 후)에서도 통과

---

### S1-09 · CI 워크플로가 실행 자체가 불가능한 구성이다

| 항목 | 내용 |
|---|---|
| **심각도** | S1 |
| **검증** | 정적 확인 (워크플로 전문 대조) |
| **위치** | `.github/workflows/ci.yml` |
| **문서 근거** | ③ 19.5 CI 게이트 · ③ 20.2 · `docs/DEPLOY_CHECKLIST.md` 배포 순서 1 |

#### 증상

현재 워크플로는 단계 나열만 있고 **실행 환경이 없다.**

| 결여 | 결과 |
|---|---|
| Postgres 서비스 컨테이너 없음 | `test:integration`, `test:e2e` 실행 불가 |
| 환경변수(`DATABASE_URL` 등) 없음 | **`test:unit` 조차 실패** — `tests/unit/p5.test.ts` 가 `@/modules/access` → `@/lib/db/client` 를 import 하고, 그 모듈이 로드 시점에 `getEnv()` 를 호출해 `환경변수 검증 실패` 예외를 던진다 |
| `pnpm db:migrate` / `db:seed` 없음 | 테이블·`platform_settings` 부재 → 레이트리밋·확정코드 설정 조회 전부 실패 |
| `playwright install --with-deps` 없음 | 브라우저 미설치 → e2e 실행 불가 |
| `build` 단계 없음 | 배포 전 빌드 검증 누락 (`next.config.ts` 도 `getEnv()` 호출) |

즉 **CI는 `selfcheck` 이후 전부 빨간불이다.** "CI 통과 후 병합"이라는 문서 19.3의 전제가 성립하지 않는다.

#### 해결 방안

```yaml
name: ci

on:
  push:
    branches: ["**"]
  pull_request:

jobs:
  verify:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: web

    services:
      postgres:
        image: postgres:17
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: interior_supporter
        ports: ["5432:5432"]
        options: >-
          --health-cmd pg_isready
          --health-interval 5s
          --health-timeout 5s
          --health-retries 20

    env:
      NODE_ENV: development
      NEXT_PUBLIC_APP_URL: http://localhost:3100
      SESSION_COOKIE_NAME: better-auth.session_token
      DATABASE_URL: postgresql://postgres:postgres@localhost:5432/interior_supporter
      BETTER_AUTH_SECRET: ci-secret-ci-secret-ci-secret-32
      BETTER_AUTH_URL: http://localhost:3100
      STORAGE_PROVIDER: r2
      STORAGE_ENDPOINT: http://localhost:9000
      STORAGE_REGION: auto
      STORAGE_ACCESS_KEY_ID: ci
      STORAGE_SECRET_ACCESS_KEY: ci
      STORAGE_BUCKET: ci
      NOTIFY_EMAIL_PROVIDER: resend
      RESEND_API_KEY: ci
      NOTIFY_FROM_EMAIL: noreply@example.com
      PAYMENT_PROVIDER: none
      CRON_SECRET: ci-cron-secret
      PLAYWRIGHT_PORT: "3100"
      TZ: UTC                      # S1-04 회귀를 CI 에서 잡기 위해 UTC 고정

    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with: { version: 11.24.0 }
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm
          cache-dependency-path: web/pnpm-lock.yaml

      - run: pnpm install --frozen-lockfile
      - run: pnpm typecheck
      - run: pnpm lint
      - run: pnpm selfcheck
      - run: pnpm db:migrate
      - run: pnpm db:seed
      - run: pnpm test:unit
      - run: pnpm test:integration
      - run: pnpm db:check
      - run: pnpm exec playwright install --with-deps chromium
      - run: pnpm test:e2e -- --grep @critical
      - run: pnpm build

      - if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: web/test-results
```

> `TZ: UTC` 를 넣는 이유 — S1-04 같은 타임존 결함이 로컬(KST)에서는 절대 안 잡히기 때문이다. CI를 UTC로 고정하면 재발을 자동으로 막는다.

**부가 개선** — 단위 테스트가 DB 모듈을 끌어오지 않도록 `tests/unit/p5.test.ts` 의 `matchesCurrentCodeVersion` import 를 `@/modules/access` 대신 순수 모듈로 분리하면 env 의존이 사라진다.

#### 완료 기준
- [ ] CI 전 단계 초록불
- [ ] 브랜치 보호 규칙에 `verify` 잡을 필수로 등록

---

## 4. S2 — 중대 결함

---

### S2-01 · 알림 디스패처가 임의의 `staff` 컨텍스트를 날조한다

| 위치 | `web/src/lib/notify/dispatch.ts:40` |
|---|---|
| **문서 근거** | ③ 6.3 (컨텍스트는 4종뿐, `platform`은 사유+감사 필수, `system`은 배치 전용) · `AGENTS.md` §2 |

```ts
function staffCtx(companyId: string, userId: string): DataContext {
  return { kind: "staff", companyId, userId, roles: ["company_admin", "project_manager"] };
}
...
const ctx = staffCtx(input.companyId, input.actorUserId ?? input.companyId);
```

**문제 세 가지**

1. 인가 판정을 거치지 않은 **최고 권한 컨텍스트**를 만들어 낸다. `emit()`에 companyId를 넘길 수 있는 코드 경로가 하나라도 사용자 입력에 노출되면 그 즉시 테넌트 경계가 무너진다.
2. `actorUserId ?? input.companyId` — 폴백으로 **회사 UUID를 userId 자리에** 넣는다. 타입은 통과하지만 의미가 완전히 틀렸다.
3. 문서가 세운 "컨텍스트는 4종, 생성 경로는 통제됨"이라는 규약이 무력화되어, 이후 생성되는 코드가 이 패턴을 복제한다.

**해결**

알림 발송은 사용자 요청이 아닌 **시스템 부수 작업**이므로 `system` 컨텍스트가 맞다. 다만 문서 6.3이 `system`을 배치 전용으로 못 박았으므로, 전용 헬퍼를 `lib/notify` 내부에만 두고 밖으로 내보내지 않는다.

```ts
// web/src/lib/notify/dispatch.ts
import { systemJob } from "@/lib/tenancy/system";

// staffCtx 제거. 모든 repo 호출을 아래로 교체한다.
const ctx = systemJob("notify-dispatch");
```

`system` 컨텍스트에서는 `scoped()` 가 `undefined` 를 반환하므로, 회사 범위가 필요한 조회(`listCompanyMembers`)는 **companyId를 인자로 명시**하도록 repo 시그니처를 바꾼다.

```ts
// membership/repo.ts
export async function listCompanyMembersFor(ctx: DataContext, companyId: string) {
  if ((ctx.kind === "staff" || ctx.kind === "customer") && ctx.companyId !== companyId) {
    throw new TenantViolationError();
  }
  return db.select(...).from(memberships).where(eq(memberships.companyId, companyId));
}
```

---

### S2-02 · 초대 수락이 수락자 본인에게 `company_admin` 컨텍스트를 부여한다

| 위치 | `web/src/modules/membership/service.ts:206` |
|---|---|
| **문서 근거** | ③ 6.3 · ③ 7.5 · ③ 12.1 (감사 행위자 정확성) |

```ts
const staffCtx: DataContext = {
  kind: "staff",
  companyId: invite.companyId,
  userId,                          // ← 초대를 수락하는 '고객' 계정
  roles: ["company_admin"],        // ← 총관리자 권한
};
```

**영향**

- 이 컨텍스트로 `insertProjectAccess` · `markInvitationAccepted` · `recordAudit` 가 실행된다.
- 감사 로그 `project_access_grant` 의 `actorId` 가 **초대한 PM이 아니라 초대받은 고객**, `actorLabel` 은 `staff` 로 남는다. 문서 ② 6장이 요구한 "누가 접근을 허용했는가"의 기록이 틀린다.
- 향후 이 경로에 역할 검사가 추가되면 고객이 통과해 버린다.

**해결**

```ts
export async function acceptInvitationForUser(userId: string, token: string) {
  const invite = await findInvitationByToken(systemJob("invite-token"), token);
  if (!invite || invite.acceptedAt || invite.expiresAt.getTime() < Date.now()) {
    return { ok: false as const, reason: "expired" as const };
  }
  const ctx = systemJob("invite-accept");          // ← system 배치 컨텍스트

  await db.transaction(async (tx) => {
    ...
    await recordAudit(
      {
        action: "project_access_grant",
        targetType: "project",
        targetId: invite.projectId,
        projectId: invite.projectId,
        after: { grantedBy: invite.invitedBy, acceptedBy: userId },   // ← 명시 기록
      },
      ctx,
      tx,
    );
  });
}
```

`recordAudit` 이 `system` 컨텍스트에서 `companyId: null` 로 남는 문제는 S2-12 수정과 함께 `companyId`를 명시 인자로 받도록 고친다.

또한 **`company_member` 초대 수락에는 감사 로그가 아예 없다.** 문서 12.2가 "멤버 초대"를 필수 기록으로 지정했으므로 수락 시점에도 `member_role_change` 를 남긴다.

---

### S2-03 · 초대 링크가 초대 대상 이메일에 묶여 있지 않다

| 위치 | `web/src/modules/membership/service.ts:200-262` |
|---|---|
| **문서 근거** | ③ 14.1 T3 · ① 6장 리스크 표 |

`invitations.email` 컬럼에 대상 주소를 저장하지만, `acceptInvitationForUser()` 는 **수락 계정의 이메일과 대조하지 않는다.** 링크를 가진 누구나 수락할 수 있다.

문서가 제시한 완화책은 "1회 수락 후 소멸 + 7일 만료"뿐이므로 **스펙 위반은 아니다.** 그러나 `kind='company_member'` 초대는 링크 유출이 곧 **업체 스태프 권한 획득**이므로 위험 크기가 다르다.

**해결 (권장)**

```ts
const account = await findAuthUser(userId);
if (invite.email && account?.email.toLowerCase() !== invite.email.toLowerCase()) {
  return { ok: false as const, reason: "email_mismatch" as const };
}
```

- `company_member` 초대는 **이메일 일치 필수**
- `project_customer` 초대는 문서 7.5가 "부부·가족 여러 계정 초대"를 허용하므로, 링크 공유 유연성을 남기되 최소한 **수락 시 알림 메일을 초대자(PM)에게 발송**한다.
- 화면 문구에 반영하고 `docs/SITE_DESIGN.md` 4.7에 한 줄 추가한다(문서가 먼저, 코드는 나중 — `AGENTS.md` §0).

---

### S2-04 · 블록 "공개 예약"이 불가능하고, 시도하면 500이 난다

| 위치 | `web/src/components/page-builder/editor.tsx` (publishAt 입력 부재) · `web/src/modules/page-builder/service.ts:395` |
|---|---|
| **문서 근거** | ② 4.2 "블록은 … 공개 예약이 가능" · ③ 5.9 I2 · `drizzle/manual/0001_checks.sql:5-7` |

**증상**

편집기 우측 패널의 상태 셀렉트는 `draft / review / scheduled / published` 4종을 모두 노출한다. 그런데 **`publishAt` 입력 필드가 편집기에 존재하지 않는다.** 블록은 항상 `publishAt: null` 로 저장된다.

기존 블록을 `review → scheduled` 로 바꾸면:
1. `assertVisibilityTransition("review", "scheduled")` 통과
2. `updateBlockRow(..., { visibilityStatus: "scheduled", publishAt: null })`
3. DB CHECK `blocks_scheduled_publish_at_chk` 위반 → **500**

> 참고: 디자인·자재·사진·일정은 `VisibilityControl` 컴포넌트(`shared/visibility-control.tsx:27-34`)가 `scheduled` 선택 시 `datetime-local` 을 요구하므로 정상이다. **블록만 누락**이다.

**해결**

```tsx
// editor.tsx — 상태 셀렉트 아래에 추가
{selected.visibilityStatus === "scheduled" ? (
  <label className="flex flex-col gap-1">
    {messages.publishAtLabel}
    <input
      type="datetime-local"
      required
      className="h-9 rounded border px-2"
      value={selected.publishAt ?? ""}
      onChange={(e) => patch(selected.id, { publishAt: e.target.value || null })}
    />
  </label>
) : null}
```

서버 측 방어도 함께 넣는다(DB CHECK에 의존하지 않는다).

```ts
// page-builder/service.ts — saveHomeBlocks 루프 안
if (block.visibilityStatus === "scheduled" && !block.publishAt) {
  throw new ForbiddenError(messages.publishAtRequired);
}
```

그리고 `publishAt` 문자열은 **S1-04의 `parseSeoulInput()`** 으로 변환한다.

---

### S2-05 · 고객이 본인 댓글을 수정할 수 없다

| 위치 | `web/src/modules/comment/service.ts:77` · `web/src/app/portal/[projectId]/requests/page.tsx` |
|---|---|
| **문서 근거** | ② 4.5 "고객은 … 본인 댓글만 수정할 수 있다" · ② 2.2 권한 매트릭스 "댓글 수정 — 고객 △(본인)" · ① 4.6 |

```ts
export async function editComment(ctx, id, body) {
  if (!isStaff(ctx)) throw new ForbiddenError();     // ← 고객 전면 차단
```

포털 요청사항 화면에도 수정 UI가 없다. 설계문서가 명시적으로 약속한 기능이 **구현되지 않았고**, 통합 테스트 `T-13("고객이 타인 댓글 수정은 403")` 은 *모든* 고객이 막혀 있어서 통과해 버려 결함을 가린다.

**해결**

```ts
export async function editComment(ctx: DataContext, id: string, body: string) {
  if (ctx.kind !== "staff" && ctx.kind !== "customer") throw new ForbiddenError();
  const parsed = z.string().trim().min(1).max(8000).parse(body);

  const row = await findCommentById(ctx, id);
  if (!row || row.deletedAt) throw new NotFoundError();
  if (row.kind === "binding") throw new ForbiddenError(messages.bindingLocked);  // 확정 지시는 불변
  if (row.authorId !== ctx.userId) throw new ForbiddenError();                   // 본인만

  const history = Array.isArray(row.editHistory) ? (row.editHistory as EditEntry[]) : [];
  return db.transaction(async (tx) => {
    const updated = await updateCommentRow(ctx, id, {
      body: parsed,
      editHistory: [...history, { body: row.body, editedAt: new Date().toISOString() }],
      editedAt: new Date(),
    }, tx);
    await recordAudit({ action: "comment_edit", targetType: "comment", targetId: id, projectId: row.projectId }, ctx, tx);
    return updated;
  });
}
```

포털 액션 추가:

```ts
// comment/actions.ts
export async function editPortalCommentAction(projectId: string, id: string, formData: FormData) {
  const ctx = await requireProjectAccess(projectId);
  await editComment(ctx, id, String(formData.get("body") ?? ""));
}
```

포털 화면에 본인 댓글일 때만 수정 폼을 렌더하고, 수정 이력이 보이도록 `편집됨 · {formatSeoul(editedAt)}` 배지를 단다(문서 ② 4.5 "수정 전후 내용과 시각은 이력으로 남는다").

**T-13 테스트도 함께 고친다** — "고객이 **타인** 댓글 수정 시도 → 403" 과 "고객이 **본인** 댓글 수정 → 성공" 두 케이스로 분리한다. 현재 테스트는 결함을 은폐하고 있다.

---

### S2-06 · 업로더가 presigned PUT에 `withCredentials`를 켜 프로덕션 R2에서 실패할 가능성

| 위치 | `web/src/components/shared/uploader.tsx:57,61` |
|---|---|
| **문서 근거** | ③ 10.1 업로드 흐름 |

```ts
xhr.open("PUT", signed.url!);
xhr.withCredentials = true;                     // ← 교차 출처 요청에 자격증명 포함
xhr.setRequestHeader("x-company-slug", companySlug);
```

- `withCredentials = true` 인 교차 출처 요청은 응답에 `Access-Control-Allow-Credentials: true` 와 **와일드카드가 아닌 구체적 Origin** 이 필요하다. R2 CORS 기본 설정은 이를 주지 않는다 → **브라우저가 차단**한다.
- `x-company-slug` 헤더는 로컬 업로드 라우트(`/api/uploads/local/[objectId]`)용인데, R2로 갈 때도 붙는다. 서명에 포함되지 않은 비-`x-amz-*` 헤더라 S3 호환 서버는 대개 무시하지만, preflight 대상 헤더가 늘어 CORS 실패 확률이 올라간다.

지금까지 검증이 `.local-storage`(로컬 파일시스템) 경로로만 이뤄져 드러나지 않았다.

**해결**

```ts
const isLocalUpload = signed.url!.startsWith("/api/uploads/local/");

const xhr = new XMLHttpRequest();
xhr.open("PUT", signed.url!);
if (isLocalUpload) {
  xhr.withCredentials = true;
  xhr.setRequestHeader("x-company-slug", companySlug);
}
for (const [key, value] of Object.entries(signed.headers ?? {})) {
  xhr.setRequestHeader(key, value);
}
```

**추가로 R2 버킷 CORS 정책을 명시하고 배포 체크리스트에 넣는다.**

```json
[{
  "AllowedOrigins": ["https://<운영도메인>"],
  "AllowedMethods": ["PUT"],
  "AllowedHeaders": ["content-type", "content-length"],
  "ExposeHeaders": ["etag"],
  "MaxAgeSeconds": 3600
}]
```

`docs/DEPLOY_CHECKLIST.md` "환경" 절에 `[ ] R2 버킷 CORS에 운영 도메인 PUT 허용` 추가.

---

### S2-07 · PM이 업체 감사 로그를 볼 수 있다 (대시보드 경유)

| 위치 | `web/src/app/app/[companySlug]/admin/page.tsx:13,17` |
|---|---|
| **문서 근거** | ② 2.2 "업체 감사로그 조회 — 총관리자 O / PM ―" · ③ 부록 C |

```ts
const ctx = await requireCompanyStaff(companySlug);   // PM 통과
const [active, byStatus, recent] = await Promise.all([
  ...,
  listAuditLogs(ctx, { limit: 10 }),                  // ← 업체 전체 감사로그
]);
```

`/admin/audit-log` 는 올바르게 `requireCompanyAdmin` 을 쓰지만, 대시보드의 "최근 활동"이 같은 데이터를 우회 노출한다. PM이 **멤버 역할 변경·계정 비활성화·업체 설정 변경** 이력까지 본다.

**해결 — 두 가지 중 택1**

(A) 역할에 따라 분기 (권장)

```ts
const ctx = await requireCompanyStaff(companySlug);
const isAdmin = hasRole(ctx, "company_admin");
const recent = isAdmin
  ? await listAuditLogs(ctx, { limit: 10 })
  : { items: [], nextCursor: null };
```
PM에게는 대신 "내 담당 프로젝트 최근 활동"을 `listAuditLogs(ctx, { actorId: ctx.userId })` 또는 프로젝트 범위로 보여준다.

(B) 대시보드에서 감사 로그 섹션을 제거하고 프로젝트 진행 요약으로 대체.

`/app/{slug}/projects/{id}/overview:18` 의 `listAuditLogs(ctx, { projectId })` 는 **프로젝트 범위**이므로 담당 PM에게 허용해도 무방하다(문서 ② 4장 "최근 업데이트"). 단 S1-01 수정 후 담당 프로젝트로 한정된다.

---

### S2-08 · `findTemplateById`에 회사 조건이 없다

| 위치 | `web/src/modules/page-builder/repo.ts:124` |
|---|---|
| **문서 근거** | `AGENTS.md` §2 "company_id 조건이 빠진 쿼리를 만들지 않는다" · ③ 17.3 |

```ts
export async function findTemplateById(ctx: DataContext, id: string) {
  const [row] = await db.select().from(templates).where(eq(templates.id, id)).limit(1);
  if (!row) return null;
  if (row.scope === "platform") return row;
  if (row.companyId) assertSameTenant(ctx, row);     // 사후 방어
  return row;
}
```

**데이터 유출은 없다** — `assertSameTenant` 가 던진다. 문제는 **어떻게 막느냐**다.

- 타 업체 템플릿 id를 넣으면 `TenantViolationError` 가 발생한다. 문서 17.3은 이 예외 **1건마다 즉시 알람**을 요구한다. 즉 사용자가 오래된 링크를 눌렀을 뿐인데 P0 알람이 울린다.
- 알람이 늘면 진짜 격리 사고 신호가 노이즈에 묻힌다.

**해결**

```ts
export async function findTemplateById(ctx: DataContext, id: string) {
  const [row] = await db.select().from(templates)
    .where(and(
      eq(templates.id, id),
      // platform scope(company_id IS NULL) 는 모두에게 읽기 허용
      ctx.kind === "staff" || ctx.kind === "customer"
        ? or(eq(templates.companyId, ctx.companyId), isNull(templates.companyId))
        : undefined,
    ))
    .limit(1);
  if (!row) return null;
  if (row.companyId) assertSameTenant(ctx, row);
  return row;
}
```

`listUsableTemplates`(`repo.ts:147`) 와 `listCompanyTemplates`(`:166`), `listPromotionQueue`(`:178`) 도 같은 방식으로 점검한다(이들은 이미 `scoped()` 를 쓰고 있어 정상).

---

### S2-09 · `rate_limit_counters` 가 무한히 증가한다

| 위치 | `web/src/lib/ratelimit/schema.ts` · `web/src/lib/cron-jobs.ts:43` |
|---|---|
| **문서 근거** | ③ 14.3 · ③ 18.4 `retention` |

`consumeRateLimit()` 은 `(key, window_start)` 마다 행을 하나씩 만들고 **아무도 지우지 않는다.** `retention` 크론은 감사 로그·세션·검증 토큰·초대만 정리한다.

로그인·파일 중계(`분당 300회`)·초대 조회까지 카운트하므로, 운영 몇 달이면 수백만 행이 되고 PK 인덱스가 비대해져 **모든 인증 요청이 느려진다.**

**해결**

```ts
// web/src/lib/ratelimit/index.ts
export async function purgeExpiredRateLimits(olderThan: Date) {
  const rows = await db.delete(rateLimitCounters)
    .where(lt(rateLimitCounters.windowStart, olderThan))
    .returning({ key: rateLimitCounters.key });
  return rows.length;
}
```

```ts
// web/src/lib/cron-jobs.ts — runRetention()
const rateLimits = await purgeExpiredRateLimits(
  new Date(Date.now() - 2 * 24 * 60 * 60 * 1000),   // 가장 긴 창(1일)의 2배
);
return { audit, sessions, verifications, invitations, rateLimits, days };
```

---

### S2-10 · 비밀번호 재설정 후 기존 세션이 살아 있다

| 위치 | `web/src/modules/membership/actions.ts:162` |
|---|---|
| **문서 근거** | ③ 14.1 T5 · ③ 7.8 (계정 정지 즉시 반영 원칙의 연장) |

`resetPasswordAction` 은 비밀번호만 바꾸고 **다른 기기·브라우저의 세션을 폐기하지 않는다.** 계정을 탈취당해 비밀번호를 바꿔도 공격자 세션이 30일 유지된다.

**해결**

```ts
export async function resetPasswordAction(_prev, formData) {
  try {
    const password = z.string().min(10).parse(formData.get("password"));
    const token = z.string().min(1).parse(formData.get("token"));
    await auth.api.resetPassword({ body: { newPassword: password, token }, headers: await headers() });

    const session = await getSession();
    if (session) {
      await deleteSessionsForUser(session.userId);      // ← 전 세션 폐기
      await recordAudit({ action: "password_reset", targetType: "user", targetId: session.userId });
    }
    redirect("/login");
  } catch (error) { ... }
}
```

`changeOwnPassword`(`lib/auth/index.ts:49`) 에도 같은 처리를 넣고, "현재 기기 제외 전체 로그아웃" 문구를 화면에 표시한다.

---

### S2-11 · 업로드 완료 처리가 파일 전체를 메모리에 적재한다

| 위치 | `web/src/modules/storage-quota/service.ts:100-101` |
|---|---|
| **문서 근거** | ③ 10.4 (PDF 50MB 허용) · ③ 16.1 성능 예산 |

```ts
const stream = await storage.get(row.objectKey);
const buf = Buffer.from(await new Response(stream).arrayBuffer());   // 전체 적재
const sniffed = sniffMime(buf);
```

매직바이트 검증에 필요한 건 **앞쪽 12바이트**뿐이다. 50MB PDF를 통째로 올리면 서버리스 함수 메모리 한도를 압박하고, 동시 업로드가 겹치면 OOM 위험이 있다.

**해결**

```ts
async function readHead(stream: ReadableStream<Uint8Array>, n = 4096): Promise<Buffer> {
  const reader = stream.getReader();
  const chunks: Uint8Array[] = [];
  let total = 0;
  while (total < n) {
    const { done, value } = await reader.read();
    if (done) break;
    chunks.push(value);
    total += value.byteLength;
  }
  await reader.cancel().catch(() => undefined);
  return Buffer.concat(chunks).subarray(0, n);
}

const head = await readHead(await storage.get(row.objectKey));
const sniffed = sniffMime(head);
if (!sniffed || sniffed !== row.mimeType) { /* quarantine */ }

// 썸네일은 이미지일 때만 전체를 읽는다 (이미지 상한 20MB)
if (sniffed.startsWith("image/")) {
  const full = Buffer.from(await new Response(await storage.get(row.objectKey)).arrayBuffer());
  ...
}
```

추가로 `sharp` 호출 전에 `sharp(full, { limitInputPixels: 100_000_000 })` 로 디코딩 폭탄을 방어한다.

---

### S2-12 · 감사 로그에 IP·User-Agent가 전혀 기록되지 않고 `actorLabel` 값이 문서와 다르다

| 위치 | `web/src/modules/audit/repo.ts:12-41` |
|---|---|
| **문서 근거** | ③ 5.8 (`ip_address`, `user_agent` 컬럼) · ② 8.2 AuditLog · ③ 14.1 T4·T5 대응 |

```ts
await (tx ?? db).insert(auditLogs).values({
  companyId: ...,
  actorId: ...,
  actorLabel: ctx?.kind,          // 'staff' | 'customer' | 'platform' | 'system'
  ...
  // ip_address, user_agent 미설정
});
```

**문제 1 — IP·UA 부재**
스키마에 컬럼이 있고 문서 ② 8.2가 명시했는데 한 번도 채워지지 않는다. 문서 17.3이 요구한 "확정 코드 검증 실패 급증 → 코드 유출 의심" 탐지가 **출처를 특정할 수 없어 불가능**하다. `verifyProjectCode` 는 `project_verifications.ip_address` 에만 IP를 남긴다(성공 시에만).

**문제 2 — `actorLabel` 불일치**
문서 5.8 주석은 `company_admin / project_manager / customer / system` 을 기대하는데 실제로는 `staff` 로 뭉뚱그려진다. 감사 화면에서 "총관리자가 한 일"과 "PM이 한 일"을 구분할 수 없다.

**해결**

```ts
export async function recordAudit(
  input: {
    action: AuditAction; targetType: string; targetId?: string; projectId?: string;
    before?: unknown; after?: unknown;
    companyId?: string;                    // system/platform 경로에서 명시
    ip?: string; userAgent?: string;
  },
  ctx?: DataContext,
  tx?: Executor,
): Promise<void> {
  const keepBody = input.action === "binding_comment";
  const label =
    ctx?.kind === "staff"
      ? (ctx.roles.includes("company_admin") ? "company_admin" : "project_manager")
      : ctx?.kind ?? "system";

  await (tx ?? db).insert(auditLogs).values({
    companyId: input.companyId
      ?? (ctx && (ctx.kind === "staff" || ctx.kind === "customer") ? ctx.companyId : null),
    actorId: ctx && "userId" in ctx ? ctx.userId : null,
    actorLabel: label,
    actionType: input.action,
    targetType: input.targetType,
    targetId: input.targetId,
    projectId: input.projectId ?? (ctx?.kind === "customer" ? ctx.projectId : undefined),
    before: keepBody ? input.before : maskPii(input.before),
    after: keepBody ? input.after : maskPii(input.after),
    ipAddress: input.ip,
    userAgent: input.userAgent,
  });
}
```

IP/UA는 요청 컨텍스트에서만 얻을 수 있으므로 **요청 스코프 헬퍼**를 둔다.

```ts
// web/src/lib/authz/index.ts
export async function requestMeta() {
  const h = await headers();
  return { ip: await requestIp(h), userAgent: h.get("user-agent") ?? undefined };
}
```

최소한 아래 행위에는 반드시 실어 보낸다: `login`, `login_failed`, `signup`, `password_reset`, `verification_success`, `verification_failed`, `verification_code_issue`, `binding_comment`, `approval`, `cross_tenant_query`.

---

### S2-13 · 로그인 실패 감사 로그에 대상 정보가 전혀 없다

| 위치 | `web/src/modules/membership/actions.ts:121-126` |
|---|---|
| **문서 근거** | ③ 7.3 · ③ 12.2 · ③ 14.1 T5 |

```ts
await recordAudit({ action: "login_failed", targetType: "user" });
```

`targetId` 없음, `actorId` 없음(세션 없음), IP 없음. **"누가 어디서 몇 번 실패했는지"를 재구성할 수 없어 기록의 목적 자체가 사라진다.**

계정 존재 여부를 노출하지 않는다는 원칙(문서 7.3)은 **사용자에게 보여주는 메시지**에 적용되는 것이고, 내부 감사 기록에는 해당하지 않는다.

**해결**

```ts
const meta = await requestMeta();
await recordAudit({
  action: "login_failed",
  targetType: "user",
  after: { emailHash: sha256(email.toLowerCase()).slice(0, 16) },   // 원문 대신 해시
  ...meta,
});
```

`AGENTS.md` §6이 "before/after에 개인정보 원문(연락처·주소)을 넣지 않는다"고 했으므로 **이메일 해시**로 남긴다. 동일 계정 반복 실패를 집계할 수 있으면서 원문은 저장하지 않는다.

---

## 5. S3 — 기능 미달·미구현

---

### S3-01 · `current_process`(현재 공정)를 설정할 방법이 없다

| 위치 | 쓰기 경로 부재 — `project/service.ts`, `project/actions.ts`, 모든 화면 |
|---|---|
| **문서 근거** | ① 4.3 "프로젝트 홈 — 현재 진행 단계" · ② 3-E · ② 5.2 (11개 공정) |

`current_process` 는 **읽기만** 된다.

| 읽는 곳 | |
|---|---|
| `portal/page.tsx:26` | 내 프로젝트 목록의 진행 단계 |
| `portal/[projectId]/home/page.tsx:29` | 고객 홈 기본 레이아웃 |
| `app/.../overview/page.tsx:32` | 내부 대시보드 |
| `page-builder/default-home.tsx:26` | 기본 홈 렌더러 |

`createProjectInput` · `updateProject` 어디에도 `currentProcess` 필드가 없다. **영구히 `null`** 이므로 고객 화면의 "현재 진행 단계"가 항상 비어 있다. 문서 ①의 핵심 가치제안("들어가면 보이는 경험") 중 가장 눈에 띄는 항목이 동작하지 않는다.

**해결**

```ts
// project/service.ts
export const updateProjectInput = createProjectInput.extend({
  currentProcess: processCategory.optional(),
  status: z.enum(["active", "paused", "done", "archived"]).optional(),
});

export async function updateProject(ctx, projectId, raw) {
  ...
  const updated = await updateProjectRow(ctx, projectId, {
    ..., 
    currentProcess: parsed.currentProcess ?? null,
    status: parsed.status ?? existing.status,
  }, tx);
  await recordAudit({
    action: "project_update", targetType: "project", targetId: projectId, projectId,
    before: { currentProcess: existing.currentProcess, status: existing.status },
    after:  { currentProcess: parsed.currentProcess, status: parsed.status },
  }, ctx, tx);
}
```

화면: `projects/[projectId]/settings/page.tsx` 에 공정 셀렉트(11종, `processLabels` 사용)와 상태 셀렉트를 추가. 사진 업로드 화면에서 "이 공정으로 현재 단계 변경" 바로가기를 두면 실사용 동선이 자연스럽다.

**연계 — S3-12(프로젝트 상태 전이)**: 현재 `archived` 로만 갈 수 있고 `paused` / `done` 전이 경로가 없다. 위 패치로 함께 해결된다.

---

### S3-02 · 자유 배치형 CMS 편집기가 골격 수준이다

| 위치 | `web/src/components/page-builder/editor.tsx` (439줄) |
|---|---|
| **문서 근거** | ① 2.3 "핵심 차별화 포인트 3 — 자유 배치형 CMS" · ① 4.5 · ② 4.2 · ③ 9장 |

이 기능은 문서 ①이 **경쟁 서비스와의 차별점으로 내세운 세 가지 중 하나**다. 현재 상태:

| 문서 요구 | 현재 구현 | 판정 |
|---|---|---|
| 블록 드래그 배치 | `@dnd-kit` 드래그 있음. 단 `delta.x / 80` **픽셀 하드코딩**(`editor.tsx:98`) → 편집 영역 폭이 달라지면 어긋남 | △ |
| **크기 조정** | 리사이즈 핸들 없음. x/y/w/h **숫자 입력 4개** | ✕ |
| 복제 | 있음 (`editor.tsx:236`) | ○ |
| 숨김 | 있음 | ○ |
| **공개 예약** | **불가 (S2-04)** | ✕ |
| 텍스트·제목 블록 | **생 HTML `<textarea>`** — 사용자가 `<p>`, `<h2>` 를 직접 타이핑해야 함 | ✕ |
| **이미지·갤러리 블록** | **`objectIds csv` 텍스트 입력** — UUID를 쉼표로 직접 입력(`editor.tsx:418-435`) | ✕ |
| Before & After 블록 | 셀렉트는 있으나 선택지가 `pairGroupId.slice(0,8)` **UUID 앞 8자리** | ✕ |
| 디자인 파일 블록 | 버전명 셀렉트 ○ | ○ |
| 자재 카드 블록 | 자재명 체크박스 ○ | ○ |
| **일정·캘린더 블록** | `ContentFields` 가 `return null` → **표시 모드·일정 타입 선택 불가** | ✕ |
| **댓글 블록** | 동일 `return null` → `allowNewComment` 토글 불가 | ✕ |
| **진행 공정 블록** | 동일 `return null` → **공정 카테고리 선택 불가**(빈 배열 = 전체 표시로 고정) | ✕ |
| **구분·여백 블록** | 동일 `return null` → spacing 고정 | ✕ |
| 버튼 블록 | 있으나 선택지가 `external / open_file / approve` **영문 원문 노출** | △ |
| 템플릿 저장·적용·승격 제안 | 있음 | ○ |
| 미리보기 | `?preview=1` 링크 있음 | ○ |

즉 **10종 블록 중 4종은 내용을 편집할 수 없고, 2종은 UUID를 손으로 입력해야 한다.**

**해결 로드맵 (우선순위 순)**

1. **갤러리 사진 피커** — `home-editor/page.tsx` 가 이미 `listPhotosByProject` 를 부를 수 있다. 썸네일 그리드에서 다중 선택하도록 교체.
   ```tsx
   // props 로 photos: Array<{id, storageObjectId, processCategory}> 를 받아
   <div className="grid grid-cols-3 gap-1 max-h-48 overflow-auto">
     {photos.map((p) => (
       <button key={p.id} type="button"
         onClick={() => toggle(p.storageObjectId)}
         className={selected.has(p.storageObjectId) ? "ring-2 ring-primary" : ""}>
         <FileImage objectId={p.storageObjectId} alt="" className="h-16 w-full object-cover" />
       </button>
     ))}
   </div>
   ```
2. **`publishAt` 입력** (S2-04)
3. **누락 4종 블록 편집기** — `schedule`(모드 라디오 + 타입 체크박스), `process`(11개 공정 체크박스), `comment`(스위치), `divider`(sm/md/lg 라디오)
4. **Before & After 선택지 라벨링** — `pairGroupId` 대신 `before/after 썸네일 + 촬영일` 로 표시
5. **리사이즈 핸들** — 우하단 드래그로 w/h 변경 (`useDraggable` 하나 더 얹으면 충분)
6. **드래그 그리드 보정** — 컨테이너 `ref` 의 `clientWidth / 12` 로 열 폭 계산
7. **텍스트 블록 최소 서식 도구** — 볼드/제목/리스트/링크 버튼 4~5개. 저장 시 `sanitize-html` 은 이미 적용됨
8. **영문 원문 한글화** (S3-08과 함께)

문서 ③ 9.1이 "편집기 난이도 리스크가 전체 일정을 흔들지 않도록 홈 한 장만"이라고 범위를 좁혀 뒀으므로, **그 한 장만큼은 완결**되어야 한다.

---

### S3-03 · plan gate(요금제 잠금 스위치)가 전혀 없다

| 위치 | `web/src/modules/billing/service.ts` = `export {}` · `gate.ts` 파일 자체 부재 |
|---|---|
| **문서 근거** | ③ 13.1 (`FEATURES`, `PLAN_MATRIX`, `featureMode`, `canUse`, `checkLimit` 명시) · ③ 13.3 · ① 5.2 "코드에 기능별 잠금 스위치를 미리 심어 기본값을 '열림'으로 두고 유료화 시점에 설정값만 바꿔 제한을 켠다" · ② 7장 · `AGENTS.md` §10 |

**현재 상태**

- `plan_enforcement_enabled: false` 가 `scripts/db-seed.mjs:27` 에 시드되어 있으나 **읽는 코드가 0곳**이다.
- `billing` 모듈은 `insertFreeSubscription` 하나뿐이고 `service.ts`·`policy.ts`·`dto.ts` 는 빈 파일이다.
- `companies.storage_quota_mb`·`storage_used_mb` 는 집계만 되고 판정에 쓰이지 않는다(이는 문서 10.6대로 **의도된 것**).

**왜 지금 만들어야 하는가**

문서 ①·②·③이 일관되게 요구한 것은 "유료화를 나중에 하되, **호출 지점(call site)은 지금 심어라**"다. 스위치가 없으면 유료화 시점에 **전 코드베이스를 다시 훑어 8개 기능의 분기점을 새로 찾아야 한다.** 지금 넣는 비용은 하루, 나중에 넣는 비용은 주 단위다.

**해결 — `web/src/modules/billing/gate.ts` (신규)**

```ts
import { getPlatformSetting } from "@/modules/company/repo";
import { getPlanTier } from "@/modules/company/repo";

export type FeatureMode = boolean | "template_only";

export const FEATURES = [
  "cms_editor", "company_template", "before_after", "purchase_tracking",
  "approval_workflow", "advanced_notification", "white_label", "api_access",
] as const;
export type Feature = (typeof FEATURES)[number];

export const PLAN_MATRIX: Record<PlanTier, Record<Feature, FeatureMode>> = {
  free:       { cms_editor: "template_only", company_template: false, before_after: false, purchase_tracking: false, approval_workflow: false, advanced_notification: false, white_label: false, api_access: false },
  starter:    { cms_editor: true, company_template: true, before_after: true, purchase_tracking: true, approval_workflow: false, advanced_notification: false, white_label: false, api_access: false },
  pro:        { cms_editor: true, company_template: true, before_after: true, purchase_tracking: true, approval_workflow: true,  advanced_notification: true,  white_label: false, api_access: false },
  enterprise: { cms_editor: true, company_template: true, before_after: true, purchase_tracking: true, approval_workflow: true,  advanced_notification: true,  white_label: true,  api_access: true },
};

/** 유료화 이전: plan_enforcement_enabled=false → 항상 허용 */
export async function featureMode(companyId: string, feature: Feature): Promise<FeatureMode> {
  if ((await getPlatformSetting("plan_enforcement_enabled")) !== true) return true;
  return PLAN_MATRIX[await getPlanTier(companyId)][feature];
}

export async function canUse(companyId: string, feature: Feature): Promise<boolean> {
  return (await featureMode(companyId, feature)) !== false;
}

export type LimitKey = "active_projects" | "pm_seats" | "storage_mb";
export type LimitResult = { allowed: boolean; limit: number | null; current: number };

export async function checkLimit(companyId: string, key: LimitKey, current: number): Promise<LimitResult> {
  if ((await getPlatformSetting("plan_enforcement_enabled")) !== true) {
    return { allowed: true, limit: null, current };
  }
  const limits = (await getPlatformSetting("plan_limits")) as Record<PlanTier, Record<LimitKey, number | null>>;
  const limit = limits[await getPlanTier(companyId)][key];
  return { allowed: limit === null || current < limit, limit, current };
}
```

**호출 지점을 지금 심는다** (전부 현재는 `true` 를 반환하므로 동작 변화 없음)

| 기능 | 심을 위치 |
|---|---|
| `cms_editor` | `page-builder/service.ts` `saveHomeBlocks()` 진입부 (`template_only` 면 템플릿 적용만 허용) |
| `company_template` | `approveTemplatePromotion()`, 회사 템플릿 저장 |
| `before_after` | `photo/service.ts` `pairPhotos()` |
| `purchase_tracking` | `material/service.ts` `changePurchaseStatus()` |
| `approval_workflow` | `design/service.ts` `setDesignApproval()`, `material/service.ts` `setMaterialApproval()` |
| `white_label` | `company/service.ts` `updateCompanySettings()` 의 `customDomain` |
| `active_projects` | `project/service.ts` `createProject()` |
| `pm_seats` | `membership/service.ts` `toggleProjectManager(enable=true)` |
| `storage_mb` | `storage-quota/service.ts` `signUpload()` |

`plan_limits` 를 `scripts/db-seed.mjs` 에 추가한다(문서 ② 7장 표 그대로).

---

### S3-04 · 화이트라벨 커스텀 도메인이 스텁이다

| 위치 | `web/src/lib/config/hosts.ts:5` — `return null` |
|---|---|
| **문서 근거** | ③ 4.5 · ③ 7.7 · ② 3-E · ② 7장 (Enterprise 전용) |

```ts
export async function resolvePortalHost(_host: string): Promise<PortalHost> {
  return null;                       // P3 이후 캐시 조회로 교체
}
```

`companies.custom_domain` 컬럼과 유니크 인덱스는 있고, `proxy.ts` 의 분기 로직도 문서대로 작성되어 있으나 **판정 함수가 항상 `null`** 이므로 커스텀 도메인 경로는 한 번도 실행되지 않는다.

Enterprise 전용 기능이고 plan gate가 꺼져 있으므로 **출시 차단 요소는 아니다.** 다만 상태를 명확히 남겨야 한다.

**해결 (둘 중 택1)**

(A) 지금 구현
```ts
import { unstable_cache } from "next/cache";
import { findCompanyByDomain } from "@/modules/company/repo";
import { systemJob } from "@/lib/tenancy/system";

export const resolvePortalHost = unstable_cache(
  async (host: string): Promise<PortalHost> => {
    if (!host || host === new URL(process.env.NEXT_PUBLIC_APP_URL!).host) return null;
    const row = await findCompanyByDomain(systemJob("portal-host"), host);
    return row ? { companyId: row.id } : null;
  },
  ["portal-host"],
  { revalidate: 300, tags: ["portal-host"] },
);
```
도메인 변경 시 `revalidateTag("portal-host", ...)`. 인증서·DNS는 Vercel 도메인 API가 필요하므로 운영 절차도 함께 문서화한다.

(B) **미구현으로 명시** — 함수에 `// TODO(S3-04): Enterprise 화이트라벨. plan gate 활성화 시 구현` 주석을 달고, `docs/ARCHITECTURE.md` 7.7에 "1차 미구현" 한 줄을 추가한다. **문서를 먼저 고치는 것**(`AGENTS.md` §0)이 중요하다. 지금 단계에서는 (B)를 권한다.

---

### S3-05 · 마케팅 페이지가 스텁이다

| 위치 | `web/src/app/(marketing)/page.tsx` (5줄) · `pricing/page.tsx` (10줄) |
|---|---|
| **문서 근거** | ② 3-A "랜딩 페이지 — 가치제안, 핵심 기능 소개, 고객 사례, CTA" · ① 7장 3단계 |

```tsx
export default function Page() {
  return <p>{messages.preparing}</p>;     // "준비 중"
}
```

파일럿 업체 3곳을 받으려면 **최소한 무엇을 하는 서비스인지 설명하는 화면**이 필요하다. 문서 ① 3장의 가치제안 표와 ② 3-A의 구성이 그대로 재료다.

**최소 구성**
- Hero: ① 1.2 한 줄 정의 + CTA("무료로 시작하기" → `/signup`)
- 문제 → 해결: ① 1.1의 4가지 문제와 ① 3장 To-Be 대응
- 기능 4블록: 디자인 버전 / 자재 조회 / 진행 사진 / 확정 코드
- 요금: ② 7장 표 + "현재 전 기능 무료 개방" 배지 (`messages.pricingFree`)
- 푸터: 약관·개인정보처리방침·문의

`/pricing` 은 ② 7장 표를 그대로 렌더하고 상단에 "현재 요금제를 적용하지 않습니다" 배너를 둔다.

---

### S3-06 · `/admin/notifications`가 "업체 알림 정책"이 아니라 "본인 개인 설정"이다

| 위치 | `web/src/app/app/[companySlug]/admin/notifications/page.tsx:16` |
|---|---|
| **문서 근거** | ② 3-C "`/admin/notifications` 알림 정책 — 인앱/이메일 발송 항목 on-off, **수신자 설정**" |

```ts
const ctx = await requireCompanyAdmin(companySlug);
const prefs = await listPreferences(ctx, ctx.userId);     // ← 로그인한 본인
```

총관리자가 `/portal/me` 에서 하는 것과 **완전히 같은 일**을 한다. 업체 차원의 정책(어떤 이벤트를 업체 전체에서 끌 것인가, 누가 받을 것인가)은 설정할 수 없다.

또한 `notification_preferences` 에는 `company_id` 가 없어 **구조적으로 업체 단위 정책을 표현할 수 없다.**

**해결 (택1)**

(A) 문서를 현실에 맞춘다 — `/admin/notifications` 를 삭제하고 `/portal/me` 로 통합, `docs/SITE_DESIGN.md` 3-C에서 항목 제거. **가장 저렴하며 지금 단계에 적합.**

(B) 업체 정책을 실제로 구현 —
```
company_notification_policy(company_id, event_type, inapp_enabled, email_enabled, recipient_role)
```
테이블을 추가하고 `dispatch.ts` 가 `개인 설정 AND 업체 정책` 으로 판정. **스키마 변경이므로 `AGENTS.md` §0에 따라 `ARCHITECTURE.md` 5장을 먼저 고쳐야 한다.**

어느 쪽이든 **현재의 "화면 이름과 실제 동작이 다른 상태"를 방치하면 안 된다.**

---

### S3-07 · 목록 대부분에 페이지네이션이 없다

| 위치 | `comment/repo.ts:9` · `design/repo.ts:8` · `material/repo.ts` · `photo/repo.ts` · `schedule/repo.ts` · `page-builder/repo.ts:147,166` · `membership/repo.ts` |
|---|---|
| **문서 근거** | ③ 4.4 R11 "목록 조회는 커서 기반 페이지네이션. OFFSET 금지" · ③ 16.2 |

커서가 구현된 곳은 audit·project·customer·company **4곳뿐**이다. 나머지는 전량 로드한다.

**가장 위험한 세 곳**

| 대상 | 예상 규모 | 영향 |
|---|---|---|
| `listPhotosByProject` | 프로젝트당 수백~수천 장(11개 공정 × 현장) | 고객 홈·사진 탭이 전부 로드 → LCP 2.5초 예산(③ 16.1) 초과 |
| `listComments` | 프로젝트당 수백 건 | 요청사항 탭 + **홈의 댓글 블록이 매 렌더마다 전량 조회** |
| `listDesigns` | 버전 누적 | 관리자 화면 지연 |

특히 `page-builder/service.ts:196-204` 의 `resolveBlocks()` 는 **블록 종류와 무관하게** `listPhotosByProject`·`listSchedules`·`listComments` 를 항상 `Promise.all` 로 호출한다. 텍스트 블록 하나만 있는 홈에서도 전체 사진·일정·댓글을 긁어온다.

**해결**

1. `lib/cursor.ts`(S1-05) 를 재사용해 위 repo들에 `{ cursor, limit }` 추가
2. `resolveBlocks()` 를 **필요한 것만 조회**하도록 변경
   ```ts
   const needPhotos = rows.some((r) => ["gallery", "before_after", "process"].includes(r.blockType));
   const needSchedules = rows.some((r) => r.blockType === "schedule");
   const needComments = rows.some((r) => r.blockType === "comment");
   const [designs, materials, pairPhotos, allPhotos, schedules, comments] = await Promise.all([
     listDesignsByIds(ctx, designIds),
     listMaterialsByIds(ctx, materialIds),
     pairIds.length ? listPhotosByPairGroupIds(ctx, projectId, pairIds) : [],
     needPhotos ? listPhotosByProject(ctx, projectId, { publishedOnly }) : [],
     needSchedules ? listSchedules(ctx, projectId, undefined, { publishedOnly }) : [],
     needComments ? listComments(ctx, projectId, { limit: 50 }) : [],
   ]);
   ```
3. 댓글 블록·요청사항 탭은 **최신 N건 + "더 보기"** 로

---

### S3-08 · 사용자에게 영문 enum이 그대로 노출된다

| 위치 | `admin/page.tsx:37` · `admin/audit-log/page.tsx` · `platform/audit-log/page.tsx` · `overview/page.tsx:51` · `admin/notifications/page.tsx:29` · `editor.tsx:408-410` · `admin/projects` 상태 |
|---|---|
| **문서 근거** | ③ 4.4 R8 "사용자 노출 문자열은 `lib/messages/` 상수를 쓴다(한국어 기본)" · `AGENTS.md` §8 |

`lib/messages/index.ts` 에 라벨 맵이 4개(`spaceLabels`, `processLabels`, `visibilityLabels`, `purchaseLabels`) 있으나 **아래가 빠졌다.**

- 감사 액션 34종 (`visibility_change`, `member_role_change`, `verification_code_issue` …)
- 알림 이벤트 16종 (`design.published`, `comment.binding_created` …)
- 일정 타입 3종 (`process` / `visit` / `confirmed`)
- 프로젝트 상태 4종 (`active` / `paused` / `done` / `archived`)
- 업체 상태 3종, 요금제 4종
- 버튼 블록 액션 3종 (`external` / `open_file` / `approve`)

결과적으로 **업체 감사로그 화면이 한국 사용자에게 읽히지 않는다.** 문서 ①이 "기록에 의한 신뢰"를 핵심 가치로 내세웠는데, 정작 그 기록을 사람이 못 읽는다.

**해결 — `web/src/lib/messages/index.ts` 에 추가**

```ts
export const auditActionLabels: Record<AuditAction, string> = {
  signup: "가입", login: "로그인", login_failed: "로그인 실패", logout: "로그아웃",
  email_verified: "이메일 인증", password_reset: "비밀번호 재설정",
  member_invite: "멤버 초대", member_role_change: "권한 변경", member_deactivate: "계정 비활성화",
  project_access_grant: "프로젝트 접근 허용", project_access_revoke: "프로젝트 접근 회수",
  verification_code_issue: "확정 코드 발급", verification_success: "확정 코드 검증 성공",
  verification_failed: "확정 코드 검증 실패",
  project_create: "프로젝트 생성", project_update: "프로젝트 수정", project_archive: "프로젝트 보관",
  visibility_change: "공개 상태 변경", approval: "승인·반려", binding_comment: "확정 지시",
  file_upload: "파일 업로드", file_delete: "파일 삭제",
  comment_edit: "댓글 수정", comment_delete: "댓글 삭제",
  template_promote_request: "템플릿 승격 제안", template_promote_approve: "템플릿 승격 승인",
  template_promote_reject: "템플릿 승격 반려",
  company_create: "업체 생성", company_status_change: "업체 상태 변경",
  plan_tier_change: "요금제 변경", subscription_cancel: "구독 해지",
  cross_tenant_query: "테넌트 경계 초월 조회",
};

export const notifyEventLabels: Record<string, string> = { /* 16종 */ };
export const scheduleTypeLabels = { process: "공정", visit: "방문", confirmed: "확정" } as const;
export const projectStatusLabels = { active: "진행중", paused: "보류", done: "완료", archived: "보관" } as const;
export const companyStatusLabels = { active: "정상", past_due: "연체", suspended: "정지" } as const;
export const planTierLabels = { free: "Free", starter: "Starter", pro: "Pro", enterprise: "Enterprise" } as const;
```

`Record<AuditAction, string>` 타입을 쓰면 **enum에 값을 추가할 때 라벨 누락이 타입 에러로 잡힌다.** 이 방식을 권한다.

---

### S3-09 · 구조화 로깅과 requestId가 전혀 없다

| 위치 | 부재 — `src` 전체에서 `console.info` 1곳(`notify/channels/email.ts:19`)이 전부 |
|---|---|
| **문서 근거** | ③ 17.1 (세 종류의 기록) · ③ 17.2 "모든 요청에 requestId를 부여하고 에러 화면에도 표시" |

문서가 요구한 `logger.info('design.published', { requestId, companyId, projectId, ... })` 형태의 기록이 **하나도 없다.** 장애 시 Vercel 기본 로그(요청 라인)만 남으므로 "특정 업체만 오류"(③ 17.4 런북) 같은 진단 절차를 수행할 수 없다.

**해결 — `web/src/lib/logger/index.ts` (신규)**

```ts
type Level = "debug" | "info" | "warn" | "error";
type Fields = Record<string, string | number | boolean | null | undefined>;

const DENY = /password|token|secret|code|email|phone|address/i;

function sanitize(fields: Fields): Fields {
  return Object.fromEntries(
    Object.entries(fields).filter(([k]) => !DENY.test(k)),
  );
}

function emit(level: Level, event: string, fields: Fields = {}) {
  // Vercel 은 stdout JSON 한 줄을 구조화 로그로 수집한다
  console[level === "debug" ? "log" : level](
    JSON.stringify({ level, event, ts: new Date().toISOString(), ...sanitize(fields) }),
  );
}

export const logger = {
  debug: (e: string, f?: Fields) => emit("debug", e, f),
  info:  (e: string, f?: Fields) => emit("info", e, f),
  warn:  (e: string, f?: Fields) => emit("warn", e, f),
  error: (e: string, f?: Fields) => emit("error", e, f),
};
```

**requestId** 는 `proxy.ts` 에서 발급해 헤더로 전파한다.

```ts
// web/src/proxy.ts
const requestId = crypto.randomUUID();
const res = NextResponse.next({ request: { headers: new Headers({ ...Object.fromEntries(request.headers), "x-request-id": requestId }) } });
res.headers.set("x-request-id", requestId);
return res;
```

서버 컴포넌트/액션에서는 `(await headers()).get("x-request-id")` 로 읽는다.

**최소 계측 지점**: 인증(로그인/가입/재설정), 확정 코드 발급·검증, 공개 상태 변경, 파일 업로드 완료·격리, 크론 4종 결과, 알림 발송 실패, 모든 `DomainError` 500.

---

### S3-10 · `TenantViolationError` 알람이 없다

| 위치 | `web/src/lib/tenancy/context.ts:34` · `web/src/lib/errors/index.ts:25` |
|---|---|
| **문서 근거** | ③ 17.3 "`TenantViolationError` 1건 → **즉시 알람** — 격리 결함 의심, 최우선 조사" · ③ 14.1 T1 |

예외를 던지기만 하고 아무 데도 알리지 않는다. 격리 사고가 발생해도 **아무도 모른다.** 문서가 위협 모델 1순위로 꼽은 항목의 탐지 수단이 없다.

**해결**

```ts
// web/src/lib/tenancy/context.ts
export function assertSameTenant(ctx: DataContext, row: { companyId: string | null }): void {
  if (ctx.kind === "system" || ctx.kind === "platform") return;
  if (!row.companyId || row.companyId !== ctx.companyId) {
    const err = new TenantViolationError();
    // 개인정보·식별자 원문은 넣지 않는다. 조사 단서만.
    logger.error("tenant.violation", {
      ctxKind: ctx.kind,
      ctxCompanyId: ctx.companyId,
      rowCompanyId: row.companyId,
    });
    reportCritical(err, { ctxKind: ctx.kind });     // Sentry captureException(level: 'fatal')
    throw err;
  }
}
```

`reportCritical` 은 `lib/observability` 에 두고 Sentry를 감싼다(벤더 격리 — `AGENTS.md` §5 정신에 맞춰 `@sentry/*` import를 한 파일로 제한).
Sentry 알림 규칙에 `tenant_violation` 태그 → 즉시 통지를 설정하고 `docs/DEPLOY_CHECKLIST.md` 에 항목을 추가한다.

`TenantViolationError` 가 상세 정보를 담지 않는 문제(S4-09)도 함께 해결한다.

---

### S3-11 · 에러 경계(`error.tsx` / `global-error.tsx`)가 없다

| 위치 | `web/src/app/` — `forbidden.tsx` 만 존재 |
|---|---|
| **문서 근거** | ③ 17.2 "에러 화면에도 requestId를 표시해 사용자가 캡처만 보내도 추적할 수 있게" |

처리되지 않은 예외가 나면 Next.js 기본 에러 화면이 뜬다. 한국어 안내도, requestId도, 복귀 링크도 없다. `not-found.tsx`, `unauthorized.tsx` 도 없다.

**해결**

```tsx
// web/src/app/error.tsx
"use client";
import { messages } from "@/lib/messages";

export default function Error({ error, reset }: { error: Error & { digest?: string }; reset: () => void }) {
  return (
    <main className="mx-auto flex max-w-md flex-col gap-4 p-8">
      <h1 className="text-xl font-semibold">{messages.errorTitle}</h1>
      <p className="text-sm text-muted-foreground">{messages.errorHelp}</p>
      {error.digest ? (
        <p className="font-mono text-xs text-muted-foreground">오류 번호: {error.digest}</p>
      ) : null}
      <button type="button" onClick={reset} className="h-9 w-fit rounded-md border px-3 text-sm">
        {messages.retry}
      </button>
    </main>
  );
}
```

`global-error.tsx`(루트 레이아웃 자체가 깨졌을 때), `not-found.tsx`, `unauthorized.tsx` 도 같은 톤으로 추가한다.
`instrumentation.ts` 에 `onRequestError` 훅을 붙여 Sentry로 서버 에러를 올린다(현재 `register()` 만 있다).

---

### S3-12 · 프로젝트 상태 전이가 `archived` 하나뿐

`project_status` enum 은 `active / paused / done / archived` 4종인데 `archiveProject()` 만 존재한다. ② 3-C "프로젝트 관리 — 상태 관리" 미구현. **S3-01 패치에 포함해 해결.**

---

### S3-13 · 알림 링크가 스태프 수신자에게도 고객 포털 URL이다

| 위치 | `web/src/lib/notify/dispatch.ts:122` |
|---|---|

```ts
const vars = { url: `${env.NEXT_PUBLIC_APP_URL}/portal/${input.projectId ?? ""}/home`, ... };
```

`design.approved`·`comment.binding_created`·`project.created`·`verification.code_issued` 등은 **담당 PM·총관리자**에게 간다. 이들은 `project_access` 가 없으므로 링크를 누르면 **403**이다. 문서 ② 6장 "관련 고객과 담당자에게 알림"의 취지가 절반만 구현됐다.

**해결**

```ts
function urlFor(eventType: string, input: EmitInput, companySlug: string, isStaffRecipient: boolean) {
  if (!input.projectId) return `${env.NEXT_PUBLIC_APP_URL}/app/${companySlug}/admin`;
  return isStaffRecipient
    ? `${env.NEXT_PUBLIC_APP_URL}/app/${companySlug}/projects/${input.projectId}/overview`
    : `${env.NEXT_PUBLIC_APP_URL}/portal/${input.projectId}/home`;
}
```

수신자별로 URL을 다르게 렌더하려면 `renderTemplate` 호출을 수신자 루프 안으로 옮겨야 한다. `companySlug` 는 `findCompanyById` 로 조회하거나 `emit()` 인자에 추가한다.

---

### S3-14 · 일정 생성·수정에 감사 로그가 없다

`createSchedule`(`schedule/service.ts:51`)·`updateSchedule`(`:73`) 어디에도 `recordAudit` 이 없다. 문서 12.2가 명시 열거하지는 않았으나 ② 6장이 "일정 변경 공지"를 관리 대상으로 삼고, 확정 일정은 분쟁 소지가 크다.

**해결** — `project_update` 액션으로 `targetType: "schedule"` 기록을 추가하고, `updateSchedule` 을 트랜잭션으로 감싼다. 자재 구매 상태 변경(`changePurchaseStatus`)도 `material_status_history` 에만 남고 감사 로그는 없으므로 함께 검토한다.

---

### S3-15 · 개인정보처리방침에 법정 필수 항목이 빠져 있다

| 위치 | `web/src/app/(marketing)/privacy/page.tsx` |
|---|---|
| **문서 근거** | ③ 15.2 · ② 9장 |

국외 이전 표(③ 15.2-1이 요구한 5개 항목)는 **정확히 들어 있다.** 그러나 개인정보처리방침으로서 아래가 없다.

- 수집·이용 목적별 항목과 보유·이용 기간
- 제3자 제공 여부
- 처리 위탁 현황(수탁자·업무 내용)
- 정보주체의 권리·의무 및 행사 방법 (③ 15.4 런북과 연결)
- 파기 절차·방법 (③ 15.3 표)
- 개인정보 보호책임자 성명·연락처
- 자동 수집 장치(쿠키) 운영
- 방침 변경 이력·시행일

상단에 `messages.legalDraft` 배너가 있어 초안임을 밝히고 있고, 문서 15.2-4가 "법률 자문을 거쳐 확정"이라 했으므로 **지금 결함은 아니다.** 다만 `docs/DEPLOY_CHECKLIST.md` "출시 전 확인"에 위 8개 항목을 체크리스트로 분해해 넣어야 누락 없이 자문에 넘길 수 있다.

---

### S3-16 · 단위 테스트 커버리지가 얕고, 일부 테스트가 결함을 은폐한다

| 항목 | 내용 |
|---|---|
| **현황** | 단위 9건 / 통합 39건 / E2E 6건 |
| **문서 근거** | ③ 20.1 (단위 40% : 통합 40% : E2E 20%) · 20.2 T-01~T-17 |

T-01~T-17은 **전부 존재한다.** 문제는 아래 두 가지다.

1. **비중 역전** — 문서는 단위 40%를 기대하는데 실제는 9건(15%)이다. `policy.ts` 가 비어 있고(S4-02) 판정 로직이 `service.ts` 에 섞여 있어 순수 함수 단위 테스트를 쓸 대상 자체가 적다.
2. **결함 은폐** — 아래 테스트는 통과하지만 실제로는 결함을 감춘다.

| 테스트 | 은폐하는 결함 |
|---|---|
| `T-13 고객이 타인 댓글 수정은 403` | 고객은 **모든** 댓글을 수정 못 하므로 통과 → **S2-05** |
| `T-12 PM이 타인 담당 프로젝트 수정 시도` | URL 접근만 검사 → **S1-01 시나리오 B** 미검출 |
| `T-02 업체 A 컨텍스트로 업체 B 리소스` | 업체 축만 검사 → **S1-01** 미검출 |
| `코드 6회 오입력은 잠긴다` | 서비스 함수 직접 호출 → **S1-03** 액션 계층 결함 미검출 |

**해결** — §8의 회귀 테스트 R-01~R-08을 추가하고, 위 4개 테스트의 어서션을 강화한다. 아울러 S4-02 해결(정책 로직을 `policy.ts` 로 추출)이 단위 테스트 비중 회복의 전제다.

---

## 6. S4 — 규약 위반·기술 부채

| ID | 항목 | 위치 | 근거 | 조치 |
|---|---|---|---|---|
| **S4-01** | repo 함수 첫 인자가 `DataContext` 가 아님 | `access/repo.ts:18` `firstProjectAccess(userId)` · `:114` `listAccessForUser(userId)` | `AGENTS.md` §2 · ③ 6.2 | 시그니처에 `ctx` 추가. `listAccessForUser` 는 `companies.status='suspended'` 필터도 없어 **정지된 업체 프로젝트가 `/portal` 목록에 계속 노출**된다 → `and(ne(companies.status,'suspended'))` 추가 |
| **S4-02** | `export {}` 뿐인 빈 파일 **32개** | `*/policy.ts` 14개, `*/dto.ts` 14개, `billing/service.ts`, `audit/service.ts` 등 | `AGENTS.md` §12 "예비 추상화, 미사용 헬퍼를 만들지 않는다" · ③ 4.3 (파일별 책임) | 둘 중 하나로 정리: **(A)** 실제로 채운다 — 각 서비스에 흩어진 `assertEditor`·`isStaff` 판정을 `policy.ts` 로, Zod 스키마를 `dto.ts` 로 이동(단위 테스트 대상이 생겨 S3-16도 해결). **(B)** 빈 파일을 삭제한다. **(A)를 권장** — 문서 4.3이 요구한 구조이고 S1-01 재발 방지에도 유리 |
| **S4-03** | 사용자 노출 문자열 하드코딩 | `lib/errors/index.ts:14,20,26,32,38,44,50,56` | R8 · `AGENTS.md` §8 | 기본 메시지를 `messages.*` 참조로 교체 |
| **S4-04** | 소프트 삭제 조회에 `isNull(deletedAt)` 없음 | `comment/repo.ts:9` `listComments` | R12 | 의도적(삭제 댓글을 "삭제되었습니다"로 표시)이므로 **예외임을 주석으로 명시**하고, `selfcheck` 가 이 규칙을 검사하게 될 경우 화이트리스트에 넣는다 |
| **S4-05** | 죽은 설정값 | `scripts/db-seed.mjs:48` `rate_limits.verify_code` | R10 단일 출처 | `verifyProjectCode` 는 `verify_attempt_limit`/`verify_attempt_window_min` 을 쓴다. 둘 중 하나로 통일 — `enforceRateLimit("verify_code", key)` 로 바꾸고 중복 설정 제거 권장 |
| **S4-06** | 로컬 개발에서 Neon 개발 브랜치 사용 불가 | `lib/config/env.ts:12-18` | ③ 18.1 "local — Neon 개발 브랜치 **또는** Docker Postgres" · `README.md` | 가드가 non-production에서 localhost만 허용한다. 문서와 충돌 → 운영 호스트 **차단 목록** 방식(`DATABASE_URL_PROD_HOST` 환경변수와 비교)이나 `ALLOW_REMOTE_DB=1` 명시 옵트인으로 완화 |
| **S4-07** | 매 호출마다 `S3Client` 생성 | `lib/storage/index.ts:8` | ③ 16.1 | 모듈 스코프 싱글턴으로 캐시 |
| **S4-08** | 크론 시크릿 비교가 비상수시간 | `lib/cron.ts:3-9` | ③ 14.4 | `crypto.timingSafeEqual` 사용 (길이 선검사 포함) |
| **S4-09** | `TenantViolationError` 가 진단 정보를 담지 않음 | `lib/errors/index.ts:25` | ③ 6.3 `new TenantViolationError({ ctxKind, rowCompanyId })` | 생성자에 `details` 인자 추가. **사용자 메시지에는 노출 금지**, 로그·Sentry 전용. S3-10과 함께 처리 |
| **S4-10** | 홈 렌더 시 불필요한 전량 조회 | `page-builder/service.ts:196-204` | ③ 16.2-2 N+1 금지 | S3-07 해결에 포함 |
| **S4-11** | 템플릿 적용 감사 로그의 `before` 가 과대 | `page-builder/service.ts:466-473` | ③ 12.1 "변경 필드만 담는다" | 블록 전체 `content` 스냅샷 대신 `{ blockCount, blockIds }` 요약으로 축소 |
| **S4-12** | PII 마스킹 규칙이 취약 | `audit/mask.ts:1` `/^(phone\|email\|address\|연락처)$/i` | ③ 12.1 · `AGENTS.md` §6 | `customerPhone`, `contactEmail`, `siteAddress`, `title`(현장명에 주소가 섞임) 등을 못 잡는다. **부분 일치**로 바꾸고 화이트리스트 방식(허용 키만 기록) 전환을 검토 |
| **S4-13** | 편집기 드래그 그리드 폭 하드코딩 | `editor.tsx:98` `delta.x / 80` | ③ 9.5 (12열 그리드) | 컨테이너 `ref.clientWidth / 12` 로 계산. S3-02에 포함 |
| **S4-14** | 개발 환경에서도 실제 메일이 발송된다 | `notify/channels/email.ts:18-27` | — | `NODE_ENV !== "test"` 면 Resend로 실발송한다. 개발 중 실수로 고객 주소에 메일이 나갈 수 있다 → `NOTIFY_EMAIL_PROVIDER=console` 옵션을 추가하고 `.env.local` 기본값으로 |

---

## 7. 수정 로드맵

각 단계는 **"이 단계를 마치면 무엇이 보장되는가"** 로 정의한다. `AGENTS.md` §11에 따라 단계당 한 번에 하나의 모듈만 손대고, 커밋은 결함 ID 단위로 쪼갠다.

### 1단계 — 파이프라인 복구 (0.5일)

> 보장: 이후 모든 수정이 자동으로 검증된다.

| 순서 | 결함 | 작업 |
|---|---|---|
| 1 | S1-07 | lint 오류 2건 + 경고 3건 제거 |
| 2 | S1-09 | CI 워크플로에 Postgres·env·migrate·seed·playwright·build 추가, `TZ: UTC` |
| 3 | S1-08 | Playwright 포트 고정, 시드의 레이트리밋 초기화, 로그인 헬퍼 어서션 |

**종료 조건**: GitHub Actions `verify` 잡 전 단계 초록불.

### 2단계 — 격리·정합성 (2~3일)

> 보장: 권한 경계가 설계대로 닫힌다.

| 순서 | 결함 | 작업 |
|---|---|---|
| 4 | **S1-01** | `projectScoped`/`assertSameProject`/`assertStaffOwnsProject` 도입, 7개 모듈 적용 |
| 5 | S1-02 | `onConflictDoNothing` |
| 6 | S1-03 | `await` 추가 + 동일 패턴 전수 점검 |
| 7 | S2-01·S2-02 | 가짜 staff 컨텍스트 제거 → `system` 전용 헬퍼 |
| 8 | S2-07·S2-08 | 대시보드 감사로그 분기, 템플릿 조회 스코프 |
| 9 | S2-03 | 초대 이메일 바인딩 (문서 먼저 수정) |

**종료 조건**: 회귀 테스트 R-01~R-04 통과.

### 3단계 — 데이터 정확성 (1~2일)

> 보장: 기록된 값이 틀리지 않는다.

| 순서 | 결함 | 작업 |
|---|---|---|
| 10 | **S1-04** | `parseSeoulInput` 도입, 6개 지점 교체, selfcheck 규칙 |
| 11 | **S1-05** | 복합 커서, 4개 repo 교체 |
| 12 | S2-12·S2-13 | 감사 로그 IP/UA/actorLabel |
| 13 | S2-09 | 레이트리밋 카운터 정리 배치 |
| 14 | S3-14 | 일정 감사 로그 |

**종료 조건**: `TZ=UTC` 로 전 테스트 통과, R-05·R-06 통과.

### 4단계 — 보안·운영 (1~2일)

| 순서 | 결함 | 작업 |
|---|---|---|
| 15 | **S1-06** | 보안 헤더 (Report-Only → 강제) |
| 16 | S2-10 | 비밀번호 변경 시 세션 폐기 |
| 17 | S2-06 | 업로더 CORS + R2 정책 문서화 |
| 18 | S2-11 | 매직바이트 헤드 읽기 |
| 19 | S3-09·S3-10·S3-11 | 로거·requestId·알람·에러 경계 |

### 5단계 — 기능 완성 (4~6일)

| 순서 | 결함 | 작업 |
|---|---|---|
| 20 | **S3-01**·S3-12 | 현재 공정·프로젝트 상태 편집 |
| 21 | S2-04·**S3-02** | 편집기: publishAt, 갤러리 피커, 누락 4종 블록, 리사이즈, 그리드 보정 |
| 22 | S2-05 | 고객 댓글 수정 + T-13 분리 |
| 23 | S3-03 | plan gate 스위치 심기 |
| 24 | S3-07 | 목록 페이지네이션 + `resolveBlocks` 조건부 조회 |
| 25 | S3-08 | 한글 라벨 맵 |
| 26 | S3-05 | 랜딩·요금제 |
| 27 | S3-06·S3-04 | 알림 정책 화면 정리, 화이트라벨 상태 명시 |

### 6단계 — 부채 정리 (2~3일)

S4 전건 + S3-16(테스트 보강) + S3-15(방침 체크리스트).

**총 예상 공수: 11~17일 (1인 기준)**

---

## 8. 추가할 회귀 테스트

수정보다 **테스트를 먼저 쓴다.** 각 테스트는 수정 전에 실패해야 한다.

| ID | 계층 | 대상 | 시나리오 | 기대 |
|---|---|---|---|---|
| **R-01** | 통합 | S1-01 | A1에만 접근·확정 권한이 있는 고객이 같은 업체 A2의 공개 디자인을 승인 시도 | `TenantViolationError` 또는 `NotFoundError` |
| **R-02** | 통합 | S1-01 | A2 담당 PM이 A2 컨텍스트로 A1 디자인의 `visibilityStatus` 변경 시도 | `ForbiddenError` |
| **R-03** | 통합 | S1-02 | 같은 계정으로 `createCompany` 2회 | 둘 다 성공, 멤버십 4건 생성 |
| **R-04** | 통합 | S1-03 | `verifyCodeAction` 을 6회 호출 | 6회째가 **던지지 않고** `{ok:false, remaining:0}` 반환 |
| **R-05** | 통합 | S1-04 | `TZ=UTC` 환경에서 `"2026-09-13T10:00"` 으로 일정 생성 | `formatSeoul(startAt)` 이 `2026-09-13 10:00` |
| **R-06** | 통합 | S1-05 | 한 트랜잭션에서 감사 5건 기록 후 `limit=2` 로 3페이지 순회 | 중복·누락 없이 5건 |
| **R-07** | 통합 | S2-05 | 고객이 **본인** 댓글 수정 → 성공 / **타인** 댓글 수정 → 403 / **binding** 댓글 수정 → 403 | 3케이스 전부 |
| **R-08** | 통합 | S2-04 | 블록을 `scheduled` 로 저장하되 `publishAt` 없음 | `ForbiddenError` (DB CHECK 도달 전 차단) |
| **R-09** | 단위 | S1-04 | `parseSeoulInput("2026-09-13T10:00").toISOString()` | `"2026-09-13T01:00:00.000Z"` |
| **R-10** | 단위 | S1-05 | `decodeCursor(encodeCursor(c))` 왕복 | 원본과 동일 |
| **R-11** | E2E | S2-07 | PM 계정으로 `/admin` 접근 | 감사 로그 섹션 비노출 |
| **R-12** | E2E | S3-01 | 현재 공정을 `tile` 로 변경 후 고객 홈 | "타일" 표시 |

**기존 테스트 강화**
- `T-12` → URL 접근 + **서비스 호출** 두 경로 모두 검사
- `T-13` → R-07로 분리·대체
- `T-02` → 업체 축 + 프로젝트 축 두 케이스

---

## 부록 A. 결함 전수 색인

| ID | 등급 | 제목 | 주 위치 | 검증 |
|---|---|---|---|---|
| S1-01 | 치명 | 프로젝트 경계 미검증 | `modules/*/repo.ts` | 🔬 재현 |
| S1-02 | 치명 | 두 번째 업체 개설 불가 | `notification/repo.ts:25` | 🔬 재현 |
| S1-03 | 치명 | 확정코드 잠금 `await` 누락 | `access/actions.ts:56` | 정적 |
| S1-04 | 치명 | 타임존 9시간 오차 | `schedule/service.ts:63` 외 5곳 | 정적 |
| S1-05 | 치명 | 커서 페이지네이션 행 유실 | `audit/repo.ts:64` 외 3곳 | 정적 |
| S1-06 | 치명 | 보안 헤더 전무 | `next.config.ts` | 정적 |
| S1-07 | 치명 | lint 실패 | 2개 파일 | 🔬 실행 |
| S1-08 | 치명 | E2E `@critical` 미통과 | `tests/e2e` | 🔬 아티팩트 |
| S1-09 | 치명 | CI 실행 불가 구성 | `.github/workflows/ci.yml` | 정적 |
| S2-01 | 중대 | 알림 디스패처 가짜 staff 컨텍스트 | `notify/dispatch.ts:40` | 정적 |
| S2-02 | 중대 | 초대 수락 가짜 admin 컨텍스트 | `membership/service.ts:206` | 정적 |
| S2-03 | 중대 | 초대 이메일 미바인딩 | `membership/service.ts:200` | 정적 |
| S2-04 | 중대 | 블록 예약공개 불가 / 500 | `editor.tsx` | 정적 |
| S2-05 | 중대 | 고객 댓글 수정 불가 | `comment/service.ts:77` | 정적 |
| S2-06 | 중대 | 업로드 `withCredentials` CORS | `uploader.tsx:57` | 정적 |
| S2-07 | 중대 | PM에게 감사로그 노출 | `admin/page.tsx:17` | 정적 |
| S2-08 | 중대 | 템플릿 조회 스코프 누락 | `page-builder/repo.ts:124` | 정적 |
| S2-09 | 중대 | 레이트리밋 테이블 무한 증가 | `ratelimit/schema.ts` | 정적 |
| S2-10 | 중대 | 비밀번호 재설정 후 세션 유지 | `membership/actions.ts:162` | 정적 |
| S2-11 | 중대 | 업로드 전체 버퍼 적재 | `storage-quota/service.ts:101` | 정적 |
| S2-12 | 중대 | 감사 IP/UA 미기록·라벨 불일치 | `audit/repo.ts:12` | 정적 |
| S2-13 | 중대 | 로그인 실패 감사 정보 없음 | `membership/actions.ts:121` | 정적 |
| S3-01 | 기능 | 현재 공정 설정 불가 | 쓰기 경로 부재 | 정적 |
| S3-02 | 기능 | CMS 편집기 골격 | `editor.tsx` | 정적 |
| S3-03 | 기능 | plan gate 전무 | `billing/` | 정적 |
| S3-04 | 기능 | 화이트라벨 스텁 | `config/hosts.ts:5` | 정적 |
| S3-05 | 기능 | 랜딩·요금제 스텁 | `(marketing)/page.tsx` | 정적 |
| S3-06 | 기능 | 알림 정책 화면이 개인 설정 | `admin/notifications` | 정적 |
| S3-07 | 기능 | 목록 페이지네이션 없음 | 7개 repo | 정적 |
| S3-08 | 기능 | 영문 enum 노출 | 7개 화면 | 정적 |
| S3-09 | 기능 | 구조화 로깅 없음 | 전역 | 정적 |
| S3-10 | 기능 | 테넌트 위반 알람 없음 | `tenancy/context.ts:34` | 정적 |
| S3-11 | 기능 | 에러 경계 없음 | `app/` | 정적 |
| S3-12 | 기능 | 프로젝트 상태 전이 불완전 | `project/service.ts` | 정적 |
| S3-13 | 기능 | 알림 링크가 스태프에게도 포털 | `notify/dispatch.ts:122` | 정적 |
| S3-14 | 기능 | 일정 감사 로그 없음 | `schedule/service.ts` | 정적 |
| S3-15 | 기능 | 처리방침 필수 항목 미비 | `privacy/page.tsx` | 정적 |
| S3-16 | 기능 | 테스트 커버리지·은폐 | `tests/` | 🔬 실행 |
| S4-01~14 | 부채 | §6 표 참조 | — | 정적 |

---

## 부록 B. 파일별 수정 지점

| 파일 | 결함 |
|---|---|
| `.github/workflows/ci.yml` | S1-09 |
| `web/next.config.ts` | S1-06 |
| `web/playwright.config.ts` | S1-08 |
| `web/src/proxy.ts` | S3-09 |
| `web/src/lib/tenancy/context.ts` | S1-01, S3-10 |
| `web/src/lib/authz/index.ts` | S1-01, S1-07, S2-12 |
| `web/src/lib/datetime.ts` | S1-04 |
| `web/src/lib/cursor.ts` *(신규)* | S1-05 |
| `web/src/lib/logger/index.ts` *(신규)* | S3-09 |
| `web/src/lib/observability/index.ts` *(신규)* | S3-10 |
| `web/src/lib/errors/index.ts` | S4-03, S4-09 |
| `web/src/lib/messages/index.ts` | S3-08 |
| `web/src/lib/cron.ts` | S4-08 |
| `web/src/lib/cron-jobs.ts` | S2-09 |
| `web/src/lib/ratelimit/index.ts` | S2-09, S4-05 |
| `web/src/lib/storage/index.ts` | S4-07 |
| `web/src/lib/config/env.ts` | S4-06 |
| `web/src/lib/config/hosts.ts` | S3-04 |
| `web/src/lib/notify/dispatch.ts` | S2-01, S3-13 |
| `web/src/lib/notify/channels/email.ts` | S4-14 |
| `web/src/lib/auth/index.ts` | S2-10 |
| `web/src/modules/access/actions.ts` | S1-03 |
| `web/src/modules/audit/repo.ts` | S1-05, S2-12, S1-07 |
| `web/src/modules/audit/mask.ts` | S4-12 |
| `web/src/modules/billing/gate.ts` *(신규)* | S3-03 |
| `web/src/modules/comment/service.ts` | S1-01, S2-05 |
| `web/src/modules/comment/repo.ts` | S1-01, S3-07, S4-04 |
| `web/src/modules/company/repo.ts` | S1-05 |
| `web/src/modules/customer/repo.ts` | S1-05 |
| `web/src/modules/design/{service,repo,actions}.ts` | S1-01, S1-04, S3-07 |
| `web/src/modules/material/{service,repo,actions}.ts` | S1-01, S1-04, S3-07 |
| `web/src/modules/membership/service.ts` | S2-02, S2-03 |
| `web/src/modules/membership/actions.ts` | S2-10, S2-13 |
| `web/src/modules/notification/repo.ts` | **S1-02** |
| `web/src/modules/page-builder/{service,repo}.ts` | S1-01, S1-07, S2-04, S2-08, S3-07, S4-10, S4-11 |
| `web/src/modules/photo/{service,repo}.ts` | S1-01, S3-07 |
| `web/src/modules/project/{service,repo,actions}.ts` | S1-05, S3-01, S3-12 |
| `web/src/modules/schedule/{service,repo,actions}.ts` | S1-01, S1-04, S3-07, S3-14 |
| `web/src/modules/storage-quota/{service,repo}.ts` | S1-01, S2-11 |
| `web/src/modules/*/policy.ts`, `*/dto.ts` | S4-02 |
| `web/src/components/page-builder/editor.tsx` | S2-04, S3-02, S4-13 |
| `web/src/components/shared/uploader.tsx` | S2-06 |
| `web/src/app/error.tsx` 외 *(신규)* | S3-11 |
| `web/src/app/(marketing)/{page,pricing}.tsx` | S3-05 |
| `web/src/app/(marketing)/privacy/page.tsx` | S3-15 |
| `web/src/app/invite/[token]/page.tsx` | S1-07 |
| `web/src/app/app/[companySlug]/admin/page.tsx` | S2-07, S3-08 |
| `web/src/app/app/[companySlug]/admin/notifications/page.tsx` | S3-06, S3-08 |
| `web/src/app/app/[companySlug]/admin/audit-log/page.tsx` | S1-04, S3-08 |
| `web/scripts/selfcheck.mjs` | S1-04 (규칙 추가) |
| `web/scripts/db-seed.mjs` | S3-03, S4-05 |
| `web/tests/**` | S3-16, §8 전건 |
| `docs/ARCHITECTURE.md` | S3-04, S3-06 (문서 우선 수정) |
| `docs/SITE_DESIGN.md` | S2-03, S3-06 |
| `docs/DEPLOY_CHECKLIST.md` | S1-06, S2-06, S3-10, S3-15 |

---

## 부록 C. 이전 문서 결과 보존 — 문서 세트 정합성 분석 (v3.0, 2026-09-12)

이 파일의 이전 판(`5_문서_문제점_분석.md`)은 **설계문서 ①②③④ 사이의 모순**을 검증했다. 그 결론은 아래와 같으며, 현재도 유효하다.

**반영된 결정**
- 고객 승인은 확정 코드를 입력한 고객만 한다. 스태프는 `approval_status` 를 대신 바꾸지 않는다. "코드 없이 모든 작업"은 업체 내부 업무(편집·공개·초대·코드 발급)로 한정한다.
- `/admin`: 대시보드·고객·프로젝트는 `requireCompanyStaff`, 멤버·설정·감사로그·템플릿·저장공간·알림 정책은 `requireCompanyAdmin`.

**v2.0 잔여 항목 처리 결과**

| 항목 | 결과 |
|---|---|
| 구모델 문장(OTP, 프로젝트 코드, 서명 URL, `User(role=…)`) | 삭제·교체됨. `/portal/{projectCode}` 중복 제목 삭제 |
| 체험(trial) 잔재 | ① 5.4, ② 랜딩 CTA·플랫폼 목록·4.6·Company 필드를 가입 즉시 `active` 로 맞춤. "체험(Trial)" 문구는 **두지 않는다** |
| ① 4.6 보안 항 공란 | 비밀번호 가입·초대·확정 코드·AuditLog 본문으로 채움 |
| 스태프 승인 대행 | ② 4.7, ③ I6·V8·8.4·헌법, ④ 헌법이 동일하게 금지 |
| `/admin` 가드 | ③ 부록 C와 ④ P3-a가 스태프 공통 / 총관리자 전용으로 일치 |

**재대조에서 새로 잡은 모순: 없음.** OTP·프로젝트 코드 열람·서명 URL 발급·trial 상태 머신을 다시 지시하는 문장은 없다. 요금 숫자·시장 추정·법률 자문 문구는 설계 모순이 아니므로 지적하지 않았다.

> 다만 **본 v1.0 분석에서 문서 자체의 보완 필요 항목 3건**이 새로 확인되었다. 코드를 고치기 전에 문서를 먼저 고친다(`AGENTS.md` §0·§11).
> 1. ③ 6.2 "방어선" 표에 **프로젝트 축**이 없다 → S1-01의 근본 원인. `company_id` 옆에 `project_id` 방어선을 명시해야 한다.
> 2. ③ 7.7 화이트라벨은 1차 미구현임을 명시해야 한다 (S3-04).
> 3. ② 3-C `/admin/notifications` 의 "수신자 설정"은 현재 스키마로 표현 불가하다. 범위를 줄이거나 스키마를 추가해야 한다 (S3-06).

---

*작성 기준: `web/` 커밋 시점 2026-09-13 · 검증 환경 Node 20 / pnpm 11.24.0 / PostgreSQL 17 / Next.js 16.3.5*

---

## 9. v2.0 수정 결과

### 9.1 수정 중 새로 발견한 결함 (v1.0 이 놓친 것)

정적 분석만으로는 잡히지 않고, **테스트를 실제로 통과시키는 과정에서** 드러났다. 넷 다 S1급이며 전부 고쳤다.

| ID | 증상 | 원인 | 왜 v1.0 이 놓쳤나 |
|---|---|---|---|
| **N-01** | **로그인이 UI 에서 한 번도 성공하지 않았다.** 올바른 비밀번호에도 "이메일 또는 비밀번호가 올바르지 않습니다" | `signInAction` 이 `signInEmail` 성공 직후 `getSession()` 을 다시 불렀다. 발급된 세션 쿠키는 **응답**에 실리는데 `headers()` 는 **요청**을 읽으므로 항상 `null` → `loginFailed` 반환 | E2E 가 이 지점에서 멈춰 "원인 불명"으로 남겼다. 서비스 함수(`auth.api.signInEmail`)를 직접 부르면 정상이라 통합 테스트로도 안 잡힌다 |
| **N-02** | 모든 403 경로가 `forbidden.tsx` 가 아니라 일반 오류 화면으로 떨어졌다 | `forbidden()` / `unauthorized()` 는 `experimental.authInterrupts: true` 가 있어야 동작하는데 `next.config.ts` 에 없었다 | `forbidden.tsx` 파일이 존재해 구현된 것처럼 보였다. N-01 때문에 E2E 가 여기까지 도달하지 못했다 |
| **N-03** | **스태프가 로그인하면 404 로 떨어졌다** | `postLoginPath()` 가 `/app/{slug}` 를 반환하는데 그 경로에 `page.tsx` 가 없었다 | 라우트 목록만 보면 `layout.tsx` 가 있어 정상으로 보인다 |
| **N-04** | **홈 편집기의 저장이 항상 "다른 사람이 먼저 저장했습니다" 로 실패했다** | 낙관적 동시성 토큰으로 `pages.updated_at` 을 JS `Date` 로 왕복시켰다. `timestamptz` 는 µs(`.968159`)까지, JS `Date` 는 ms(`.968`)까지 담으므로 `=` 비교가 영원히 실패한다 | `saveHomeBlocks` 를 덮는 테스트가 하나도 없었다. S1-05(커서)와 같은 뿌리인데 v1.0 은 커서 쪽만 지적했다 |

> **교훈** — N-01·N-04 는 "그 경로를 실제로 실행해 보는 테스트가 없었기 때문에" 존재했다.
> v1.0 의 S3-16(테스트가 결함을 은폐한다)이 지적한 문제가 실제로 얼마나 비싼지 보여준다.

### 9.2 남은 항목

고치지 않은 것과 그 이유를 명시한다. 숨기지 않는다.

| ID | 항목 | 상태 | 사유 |
|---|---|---|---|
| S3-04 | 화이트라벨 커스텀 도메인 | **의도적 미구현으로 문서화** | Enterprise 전용 + plan gate 비활성. `ARCHITECTURE.md` 7.7 에 "1차 범위에서는 구현하지 않는다"와 켤 때 필요한 작업을 명시했다. `resolvePortalHost()` 는 스텁으로 남기고 그 사실을 주석에 적었다 |
| S3-15 | 개인정보처리방침 법정 필수 항목 | **미해결** | 법률 자문 영역이다(문서 15.2-4). 코드로 결정할 수 없다. `DEPLOY_CHECKLIST.md` 의 출시 전 확인 항목으로 남긴다 |
| S4-02 | `export {}` 뿐인 빈 파일 32개 | **미해결 — 사용자 확인 대기** | `AGENTS.md` §11 이 "기존 파일을 지우거나 크게 옮기기 전에 먼저 알린다"고 정하고 있어 임의로 삭제하지 않았다. 선택지는 두 가지다: **(A)** 각 service 에 흩어진 정책 판정(`assertEditor` 등)을 `policy.ts` 로, Zod 스키마를 `dto.ts` 로 옮긴다(문서 4.3 이 요구한 구조이며 단위 테스트 대상이 생겨 S3-16 도 함께 풀린다). **(B)** 빈 파일을 삭제한다. **(A) 권장** |
| S3-07 | 목록 커서 페이지네이션 | **부분 해결** | 감사·프로젝트·고객·업체는 (created_at, id) 복합 커서로 교체했고, 댓글·디자인·자재·사진에는 상한(limit)을 걸어 전량 로드를 막았다. 이들의 "더 보기" UI 는 아직 없다 |
| S3-02 | 홈 CMS 편집기 | **실사용 가능 수준까지** | 사진 피커·공개 예약·누락 4종 블록 편집기·리사이즈 손잡이·그리드 보정을 넣었다. 텍스트 블록은 여전히 HTML 입력이다(서식 도구 미구현) |

### 9.3 해결 요약 — 무엇을 어떻게 고쳤나

#### 격리 (S1-01 · I11 · I12)

격리 경계를 **두 축**으로 재정의했다. 문서(`ARCHITECTURE.md` 5.9 I11·I12, 6.2 방어선 2-B, 6.3, `AGENTS.md` §2)를 먼저 고치고 코드를 맞췄다.

```ts
// lib/tenancy/context.ts
projectScoped(ctx, table)      // 고객 컨텍스트는 자기 프로젝트 밖을 조회하지 못한다
assertSameProject(ctx, row)    // 꺼낸 행이 그 프로젝트인지 재확인
// lib/authz/index.ts
assertStaffOwnsProject(ctx, projectId)  // PM 단독 권한자는 담당 프로젝트만
```

design · material · photo · schedule · comment · access · page-builder · project 의
모든 단건 조회·수정에 두 축을 적용했다. `selfcheck` 가 누락을 잡는다.

#### 정확성 (S1-04 · S1-05 · N-04)

- `lib/datetime` 에 `parseSeoulInput` / `parseSeoulDayEnd` / `seoulMonthRange` 추가. 일정·예약 공개·감사 필터·월 범위 전부 교체.
- `lib/cursor` 신설. 커서에 **DB 가 만든 µs 문자열**을 담고 `::timestamptz` 로 되돌려 비교한다. JS `Date` 를 커서에 쓰지 않는다.
- CI 를 `TZ=UTC` 로 고정해 시간대 결함이 개발 환경(KST)에 숨지 않게 했다.

#### 컨텍스트 위조 제거 (S2-01 · S2-02)

알림 디스패치와 초대 수락이 `staff`/`company_admin` 컨텍스트를 날조하던 것을 `system` 배치 컨텍스트로 바꾸고, 업체 범위가 필요한 조회는 `companyId` 를 인자로 명시해 받도록 repo 시그니처를 바꿨다(`listCompanyMembersFor`, `findMembershipIn`).

#### 그 밖

보안 헤더 6종, requestId 전파, 구조화 로깅, 테넌트 위반 즉시 알람, 에러·404·403·401 화면, 레이트리밋 카운터 정리 배치, 비밀번호 변경 시 전 세션 폐기, 업로드 헤드만 읽는 매직바이트 검증, 업로드 CORS 분기, 감사 IP/UA·실제 역할 라벨, 한글 라벨 맵 6종, 현재 공정·프로젝트 상태 편집, plan gate(`billing/gate.ts` + 호출 지점 9곳), 랜딩·요금제 화면.

### 9.4 추가된 회귀 테스트

`tests/integration/boundary.test.ts` (12건) · `tests/unit/{datetime,labels}.test.ts` (10건)

| 대상 | 검증 |
|---|---|
| T-18 | 프로젝트 A 확정 고객이 같은 업체 프로젝트 B 디자인 승인 → 거부 |
| T-19 | PM 이 담당 아닌 프로젝트 디자인 공개 → 403 |
| T-20 | 한 계정이 업체 두 개 개설 → 둘 다 성공 |
| T-21 | `TZ=UTC` 에서 `2026-09-13T10:00` 입력 → KST 10:00 으로 표시 |
| T-22 | 한 트랜잭션의 감사 5건을 `limit=2` 로 순회 → 누락 없이 5건 |
| T-23 | 고객 본인 댓글 수정 성공 / 타인 403 / 확정 지시 403 |
| — | 홈 블록 저장 성공 + 오래된 토큰은 충돌 (N-04) |
| — | 예약 공개 블록에 시각 없으면 저장 거부 (I2) |
| — | 감사 `actor_label` 이 `company_admin` / `project_manager` 로 갈린다 |
| — | 커서가 µs 를 보존한다 |
| — | 라벨 맵이 enum 을 빠짐없이 덮는다 |

**기존 테스트의 은폐 문제**도 정리했다. `T-13`("고객이 타인 댓글 수정 → 403")은 고객이 *아무* 댓글도 수정할 수 없어서 통과하던 것이었는데, 이제 T-23 이 허용 방향을 함께 검증하므로 진짜 회귀 테스트가 되었다.

### 9.5 selfcheck 에 추가한 회귀 방지 규칙

```
1. actions/service 에서 입력 문자열을 new Date() 로 파싱 금지 → parseSeoulInput() (5.1)
2. actions.ts 의 try 블록 안에서 await 없이 Promise 반환 금지 (catch 무효화)
3. project_id 를 가진 테이블의 repo 에 projectScoped()·assertSameProject() 필수 (I11)
```

### 9.6 함께 고친 설계문서

코드보다 문서를 먼저 고쳤다(`AGENTS.md` §0 · 문서 19.1-6).

| 문서 | 변경 |
|---|---|
| `docs/ARCHITECTURE.md` = `3_아키텍처_문서.md` | 5.9 에 I11·I12 추가 / 6.2 방어선을 두 축으로 재작성 / 6.3 에 `projectScoped`·`assertSameProject` / 6.4 에 `assertStaffOwnsProject` / 7.5 초대 이메일 바인딩 / 7.7 화이트라벨 1차 미구현 명시 / 11.2 알림 컨텍스트·수신자별 링크 / 12.1 감사 시그니처·IP·역할 라벨 / 14.2 헤더·세션 폐기 / 14.3 카운터 정리 / 16.2 복합 커서·선조회 금지 / 17.2 로깅·에러 화면 / 17.3 알람 / 18.1 DB 가드 차단 목록 / 19.4 selfcheck / 19.5 CI 실행 환경 / 20.2 T-18~T-23 / 3.2 Next 16 규칙 3종 / 부록 A·B·C |
| `docs/SITE_DESIGN.md` | 3-C `/admin/notifications` 범위 축소 / 4.7 초대 이메일 바인딩·프로젝트 경계 / 6장 알림 수신자 고정·링크 분기 / 9장 격리 두 겹 |
| `AGENTS.md` | §1 Next 16 규칙 3종 / §2 두 축 격리 + staff 컨텍스트 조립 금지 / §4 고객 댓글 수정·초대 이메일 / §8 라벨 맵·복합 커서·시간대·try/await |
| `docs/DEPLOY_CHECKLIST.md` | R2 CORS, 보안 헤더, 알람 설정, 개인정보처리방침 항목 |

---

*v2.0 검증 기준: Node 20 / pnpm 11.24.0 / PostgreSQL 17 / Next.js 16.3.5 / TZ=UTC*
