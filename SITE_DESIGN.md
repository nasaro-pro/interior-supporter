---
title: "인테리어 프로젝트 관리 플랫폼 — ② 총 사이트 설계문서"
version: "v2.0"
date: "2026-09-15"
source: "2_총_사이트_설계문서_2차_설계안.pdf"
---

# ② 총 사이트 설계문서 (v2.0)

문서 유형: 총 사이트 설계문서 (2/4). 선행: ① 아이디어 가이드. 하위: ③ 아키텍처.

## 0. 개요

멀티테넌트 SaaS로, 인테리어 업체가 고객별 프로젝트를 도안·자재·요청·사진·일정·견적·미팅 기록으로 운영한다. 2차는 초안의 골격을 유지하고 **책임 주체·접근 경계·파일럿 범위**를 고정한다.

### 용어

| 용어 | 정의 |
| --- | --- |
| 업체 | 구독 시공사. 데이터 격리 단위 (`company_id`) |
| 플랫폼 운영자 | SaaS 운영 주체. 별도 관리자 환경·MFA |
| 업체 총관리자 | 구성원 초대·배정·업체 현황 |
| 디자이너·PM | 담당 프로젝트 운영·공개 |
| 시공팀 | 배정 현장의 작업일지·사진 기록. 개인 계정 |
| 고객 | 초대된 계정의 공개 콘텐츠 열람·요청·(지정 계정) 확정 |
| 프로젝트 배정 | 구성원에게 프로젝트·역할·기간을 부여 (`ProjectAssignment`) |
| 블록 | 고객 홈 CMS 단위. 1차는 12컬럼 그리드+프리셋, 전면 프리폼은 후속 |
| 공개 상태 | 초안 / 검토 / 예약 / 공개 |
| 확정 코드 | 지정 의사결정자의 도안·자재 확정 권한. 해시 저장, 재발급 시 무효 |
| 멤버십 | 계정의 업체 역할. 한 계정이 여러 업체에 다른 역할 가능 |

### 설계 원칙

1. 테넌트 격리: 앱 검사 + PostgreSQL RLS (파일럿 전 필수)
2. 배정 기반 접근: 같은 업체여도 미배정 프로젝트 불가. 시공팀은 기간 있는 배정만
3. 공개 상태 명시. 고객은 `published`만
4. 덮어쓰지 않음: 버전(도안·자재·견적), 요청 원문+정정, 일지 수정 이력
5. 개인 계정만. 공용 아이디 금지
6. 파일럿 우선: 실제 프로젝트 1건을 2~4주 운영할 흐름이 1차 완료 기준

## 1. 역할과 권한

| 역할 | 코드 | 주요 권한 | 불가 |
| --- | --- | --- | --- |
| 플랫폼 운영자 | `users.is_platform_admin` | 업체 상태·정책·전체 감사. 업무 데이터는 사유+감사 | 사유 없는 교차 조회 |
| 업체 총관리자 | `company_admin` | 초대·배정·전체 프로젝트·감사 | 고객 대신 확정 |
| 디자이너·PM | `project_manager` | 담당 프로젝트 7기능·공개·초대 | 미배정 프로젝트 |
| 시공팀 | `field_worker` | 배정 현장 일지·사진·완료 | 견적·권한·고객 승인 |
| 고객 | `project_access` | 공개분 조회·요청. 지정 계정만 확정 | 초안, 원문 삭제 |

메뉴 노출과 별개로 매 요청에서 업체·역할·배정/접근을 서버가 재검증한다. 스태프는 `approval_status`를 대신 바꾸지 않는다.

## 2. 정보구조

로그인 체계는 하나. 소속·역할·배정으로 분기한다.

- 마케팅: `/` `/pricing` `/signup` `/login` `/terms` `/privacy` `/contact` `/onboarding/company`
- 플랫폼: `/platform/*` (파일럿 후 `admin.` 서브도메인·별도 세션 권장, MFA는 파일럿 전)
- 업체 관리: `/app/{slug}/admin/*` — 대시보드, 고객, 프로젝트, 구성원, **현장 배정**, 템플릿, 저장공간, 감사, 설정
- PM: `/app/{slug}/projects/{id}/*` — 개요, 홈 편집, 도안, 자재, 요청, 사진, 일정, **견적**, **미팅**, 참여자, 설정
- 시공팀: `/app/{slug}/field/*` — 오늘 작업, 내 현장, 공정일지, 사진, 알림
- 고객: `/portal/*` — 홈, 도안, 자재, 요청, 사진, 일정, 견적, 미팅, 확정 코드

시공팀만 있는 계정은 `/app/{slug}`에서 `/field`로 보낸다. `/admin`·프로젝트 워크스페이스 탭에 들어올 수 없다.

## 3. 화면 상세

### 3.1 가입·개설

계정 가입과 업체 개설 분리. 개설자 = 총관리자+PM. 즉시 `active`. 체험 없음. 요금제 미적용.

### 3.2 고객 홈 편집기 (1차)

홈만 CMS. 7개 상세는 고정 양식. 12컬럼 그리드에서 이동·리사이즈·복제·숨김·공개/예약. 프리셋 3종(Atelier, Gallery, Process Journal). 블록 `style`: 배경·패딩·타이포·오버레이·모서리·최대폭. 실행 취소/다시 실행, 데스크톱·태블릿·모바일 미리보기. 전면 프리폼 캔버스는 파일럿 후.

### 3.3 일곱 기능

1. 도안: 버전, SKP·DWG는 다운로드만, PDF·이미지 미리보기, Before & After
2. 자재: 공간 7종, 구매 견적→완료, 지정 계정 확정. 확정 후 변경은 새 버전+사유
3. 고객 요청: **원문 수정·삭제 금지**, 정정 기록 추가, 스태프 답변·처리 상태. 확정 지시는 확정 코드 후 별도 배지
4. 시공 사진: 공정 11종, 작업일지 연결, 원본+WebP, 공개분만 고객
5. 일정: 가로 공정 타임라인 + 일요일 시작 월간 캘린더. 공정·방문·확정·미팅
6. 견적: PDF 버전, 교체 금지, 고객 공개본 다운로드
7. 미팅: 날짜·참가자·회의록·첨부·결정사항. 공개분에 고객 확인 의견

### 3.4 시공팀

배정 프로젝트·기간만. 오늘 작업 → 공정 → 사진 → 특이사항 → 완료. 수정 시 전후값 이력.

### 3.5 확정 코드

초대 = 열람·요청. 코드 입력 = 도안·자재 확정. 재발급 시 기존 확정 권한 전부 무효.

## 4. 분류

공간: 거실·주방·욕실·침실·조명·가구·가전. 공정: 철거·전기·목공·타일·필름·마루·도기셋팅·도배·가구·조명설치·마감.

## 5. 알림·감사

알림(인앱+이메일): 도안 공개, 고객 요청, 일정 변경, 확정 요청, 사진 공개, 배정 변경. 알림톡은 슬롯만.

감사: 가입·로그인·배정·접근·공개·확정·업로드·일지 완료·운영자 교차 조회. 일반 사용자는 감사 수정·삭제 불가.

## 6. 요금제

설계만. `plan_enforcement_enabled=false`. 전 기능 개방.

## 7. 데이터 (개념)

User — Membership(`company_admin`/`project_manager`/`field_worker`) — ProjectAssignment(`designer`/`field_worker`, 기간) — ProjectAccess — ProjectVerification.

Project 1:N Page/Block, DesignVersion, Material, CustomerRequest+Correction, ProgressPhoto, FieldWorkLog, Schedule, EstimateVersion, MeetingRecord+Participant, FileObject.

FileObject는 원본과 `derivative_of_id` WebP 파생본.

## 8. 비기능

격리: Repository + RLS. 고객 포털 LCP 2.5s / TTFB 600ms. 파일 MIME·매직바이트·용량. SKP/DWG는 실행 없이 저장·다운로드. 고객·시공팀 모바일 필수.

## 9. 릴리즈

1단계 안전한 기반 → 2단계 파일럿 핵심 → 3단계 2~4주 실사용 → 4단계 피드백(프리폼 캔버스, CAD 뷰어, 간트, 알림톡). 전 기능 완성 후 공개가 아니다.
